# O&O 기획 정리

> 출처: Notion `[중앙해커톤 1팀 페이지]`(공개 링크) 및 하위 페이지 전체 + 워크스페이스 `API 명세서/` 로컬 export
> 정리일: 2026-08-12 · 정리: 백엔드(DRF) 담당
> 이 문서는 **기획 스냅샷**이다. API 필드 단위 진실 공급원은 `API 명세서/`(v0.5, 2026-08-11) 이다.

---

## 1. 한 줄 정의

**로그인 없이** 오프라인 명품 매장을 구경한 사람의 행동(무엇을 봤나 · 얼마나 머물렀나 · 챗봇에 뭘 물었나)을 모아,
관람이 끝나면 **개인 취향 리포트 + 추천 상품**을 만들어 주고, 같은 데이터를 **브랜드에 파는 B2B 자산**으로 쓰는 서비스.

- 대상 브랜드(데모 기준): **MCM**, 매장은 **하나로 고정**
- 백엔드: **Django REST Framework**, 서버는 **stateless**(세션·대화 상태를 메모리에 두지 않고 매 요청 DB 조회)

---

## 2. 문제 정의 (자료 창고 → "파악한 문제점")

### 2.1 MCM의 포지션 문제
- 세계 100대 명품 브랜드에 한국 기업으로 유일하게 진입(60위권)하지만, 체감 포지션은 하이엔드 아래의 **애매한 중간 지대**.
- 과거 주 고객층(현재 50대)은 "애매한 브랜드 대신 돈 모아 하이엔드"로 이동.
- 2030은 무신사·크림·번개장터·당근 등으로 **하이엔드 접근성이 좋아져** 굳이 이 브랜드를 찾을 이유가 사라짐.
- 가격대(기본 100만원선)가 선뜻 지르기도, 중고로 되팔기도 애매함 → 잔존가치 손해.

### 2.2 브랜딩 문제
- 과거 명품 브랜딩 실패의 미련에서 못 벗어남(가격·브랜딩 전략 모두 과거형).
- 2030 검색은 **백팩 / 백참·키링** 등 특정 효자 카테고리에 편중. 풀라인업으로 유입이 고르게 퍼지지 않음.
- 지수 비교(2030 관심도): 메가 명품(샤넬·루이비통 등) 100 / 컨템포러리(마르지엘라·아미 등) 40~70 / **MCM 15~25**.
- 해외: 중국 인기 소강, 일본은 특정 서브컬처 소비 이미지로 고착. 다만 **바이럴 마케팅이 잘 먹히는 브랜드**임이 확인됨(아이돌 착용 효과). 문제는 그 효과가 일반 소비자까지 내려오지 않는다는 점.
- 남은 자산: **Y2K·스트리트 감성 마니아층(핵심 팬 10~20%)의 견고한 유입**.

### 2.3 그래서 왜 이 서비스인가
- 브랜드는 **구매 결과 데이터는 있지만 구매 이전의 관심·고민 데이터가 없다.**
- 팝업스토어/전시형 매장은 인건비·공간대여료를 쓰면서도 방문자 데이터를 거의 못 건진다.
- O&O는 **전시형 매장 경험 + AI 직원(챗봇)** 으로
  1) 인건비를 줄이고
  2) 온라인에 흩어져 있던 잠재고객의 **구매 이전 데이터**를 확보한다.

### 2.4 1차 회의에서 걸러낸 것 (자료 창고 → "생각해볼거", 1차 회의록)
- ❌ **메타버스**: 시장에서 사장된 기술. input > output. 기업 유지비만 늘어남 → 채택하지 않음.
- ⚠️ **AI를 어디에 쓰는지 방향성이 불명확**했던 초기 아이디어 → **AI 사용 지점을 2곳으로 못 박아 해결**(§6).
- ⚠️ 매장 평면 데이터를 서버가 관리하면 실현 가능성이 흐려짐 → **맵은 프론트가 직접 제작**, 서버는 논리적 구성만 제공.
- ⚠️ 분석 결과의 신뢰성 → **근거(reason)와 confidence를 항상 함께 제공**하는 것으로 해결.

---

## 3. 비즈니스 모델 (B2B)

| 항목 | 내용 |
| --- | --- |
| 판매 대상 | 브랜드(매장 운영 주체) |
| 파는 것 | 구매 **이전**의 관심·비교·망설임 데이터 + 코호트 인사이트 |
| 차별점 | 결제 로그가 아니라 "무엇을 오래 봤고, 무엇과 비교했고, 무엇을 물어봤는가" |
| 부수 효과 | AI 직원이 응대 → 인건비 절감, 전시형 매장으로 브랜드 경험 강화 |
| 프라이버시 | 개별 익명 사용자 추적이 아니라 **집계·코호트 단위**로만 제공 → 재식별 위험 감소 |

---

## 4. 사용자 여정 (확정)

```
메인 화면 "입장" 버튼
  → 익명 UUID 확인/발급 + Visit 생성 + 연령대·성별(선택, 건너뛰기 허용)
  → 전시존(매대) 목록 + 각 존의 상품 목록 수신
  → 전시존 탐색 · 상품 상세 조회(이미지/가격/색상/소재/크기/수납력/스토리)
  → 상품 클릭 시 채팅 타임라인에 액션 메시지 자동 적립
  → 프리셋 버튼(가격 · 재질 · 디자인 의도) 즉시 확인
  → 안 풀리면 "채팅으로 대화하기" → LLM 챗봇 (문맥 유지)
  → "관람 끝" → slug 즉시 발급(분석은 비동기)
  → 리포트 열람: 취향 키워드 → 최적 상품 1개 크게 → 추천 상위 N개 그리드
  → '자세히 보기' / '구매하기(본사 사이트 이동)'
  → slug 링크로 집에서도 재확인·구매

[이탈 복구]
관람 중 창을 실수로 닫음 → 다시 접속
  → 같은 UUID로 진행 중 Visit(미종료)이 남아 있으면 그 Visit을 그대로 이어준다
  → "이어서 볼까요? / 새로 시작" 선택
  → 이어하면 그때까지의 채팅 타임라인·행동 기록이 살아 있고, "관람 끝"을 눌러 정상적으로 리포트를 받는다
```

핵심 결정 4개:
1. **로그인 없음.** 익명 UUID + visit_token만 사용.
2. **연령대·성별은 입장 시점에 받는다.** (리포트 직전이 아니라) → 분석 시작부터 코호트 분류 가능 + 관람 후 입력 요구가 없어 이탈 감소.
3. **퇴장 설문은 받지 않는다.** (구버전 v0.4에는 있었으나 최신 스펙에서 제외 확정)
4. **진행 중 Visit은 이어서 할 수 있다.** 익명 UUID를 쓰는 이유가 여기 있다 — 로그인 없이 쓰는 편의성 + **퇴장을 누르기 전에 잘못 껐어도 다시 들어오면 그대로 이어져야** 한다. 이어한 뒤 "관람 끝"을 눌러 리포트를 받는다. (§11의 "재방문 로직 제거"와 다른 기능이다. 상세는 §11 참조)

---

## 5. 챗봇 = 대화창이 아니라 "타임라인"

O&O 챗봇의 핵심은 **클릭이 그대로 메시지로 쌓인다**는 것.

- 진열대 클릭 → `"1번 진열대 클릭"` 말풍선
- 상품 클릭 → `"Heritage 백팩 상품 클릭"` 말풍선
- 프리셋 열람 → 미리 작성해 둔 문구(LLM 아님)를 그대로 표시

효과:
- 사용자가 "채팅으로 대화하기"로 넘어갔을 때 **문맥이 이어진다.** 상품명을 말하지 않고 "이 상품의 디자이너와 의도를 설명해줘"가 가능.
- 프리셋은 **즉시 뜨고 · 비용 0 · 브랜드가 문구를 통제**. LLM 비용이 드는 구간을 최소화.
- 어떤 프리셋을 열어봤는지(`preset_view` + `preset_key`)도 **취향 신호**로 기록.

표시 문구는 **클라이언트가 보내지 않고 서버가 생성한다.**

`role` 값: `assistant`(챗봇 곰) / `user_action`(클릭 자동 메시지) / `user`(직접 타이핑) / `preset`(프리셋 답변)

---

## 6. AI를 쓰는 지점은 정확히 2곳

### AI ① 컨텍스트 챗봇 — `POST /api/v1/chat`
- OpenAI API(예정). 키는 서버에 숨기고 LLM 게이트웨이에서 모델 교체·재시도·토큰 사용량 통제.
- 응답은 **SSE 스트리밍**(`data: {"delta": "..."}` → `data: {"done": true, ...}`).
- 매 요청 서버가 문맥을 조립: `chat_logs`의 최근 메시지 → 최근 클릭된 진열대·상품 → 그 상품의 `llm_context`/`features` → `age_band`/`gender`로 톤 조절.
  - 클라이언트가 `context`를 명시로 보내면 그 값이 우선.
- 답변 원칙: **확인 가능한 상품 데이터에만 근거.** 재고처럼 변하는 정보는 단정하지 않음.
- 부수 목적(중요): 대화에서 **긍정·부정 취향과 구매 의도 추출** → AI ②의 재료. 클릭·체류만으로는 안 드러나는 **망설임**을 잡는 통로.

#### 챗봇 추천 표시 = 카드 + 텍스트 병행 (2026-08-12 확정)
A안/B안 중 택일이 아니라 **둘 다** 쓴다. 같은 추천을 두 가지로 표현한다.

| 층 | 형태 | 근거 |
| --- | --- | --- |
| 텍스트 | 스트리밍 답변 본문에 위치 안내 문장을 섞는다 — "3번 진열대 2번 상품에 있어요" | 오프라인 매장이므로 **실물을 찾아갈 수 있어야** 한다. `/enter`가 내려준 `no`(진열대·상품 번호)를 그대로 사용 |
| 카드 | 스트리밍 종료 이벤트에 `recommendations[]`를 실어 카드로 렌더링 | 바로 상세로 넘어가는 클릭 경로 + 추천 클릭률 측정 |

```
data: {"delta": "그 무드라면 "}
data: {"delta": "3번 진열대 2번 상품이 잘 맞아요."}
data: {"done": true, "message_id": "m_55",
       "recommendations": [
         { "product_id": "p_204", "name": "Stark 백팩", "thumbnail": "https://.../t.jpg",
           "scene_no": 3, "product_no": 2, "reason": "선호 소재(레더)와 일치" }
       ]}
```

- 카드에도 `scene_no` / `product_no`를 함께 내려준다 → 카드와 문장이 같은 번호를 가리켜야 어긋나지 않는다.
- 텍스트 문장은 LLM이 생성하지만 **번호·상품은 서버가 고른 `recommendations`에서만** 나오게 프롬프트에 제약을 건다. LLM이 없는 상품 번호를 만들어내면 안 된다.
- 카드가 화면에 뜨는 순간 `recommendation_impression`, 누르면 `recommendation_click` 이벤트를 남긴다(텍스트만 읽고 넘어간 경우와 구분됨).

### AI ② 리포트·데이터 생성 (비동기 워커)
`POST /api/v1/visits/{visit_id}/finish` 가 큐에 넣은 작업을 워커가 처리.

| 단계 | 하는 일 | 왜 |
| --- | --- | --- |
| ① 이벤트 집계 | 상품·전시존별 조회·재조회·체류·챗봇 수 | 수백 개 이벤트를 상품 단위로 압축 |
| ② 관심도 산출 | log 가중합 | 한 상품만 30번 본 사람이 프로필을 지배하지 않게 |
| ③ confidence | 신호 총량 평가 | 데이터가 적을 때 틀린 개인화 회피 |
| ③′ 취향 추출 (LLM) | 챗봇 대화를 구조화 태그로 | 행동에 안 드러나는 망설임 포착 |
| ④ 취향 벡터 | 관심 상품의 카테고리·색상·소재·스타일·가격대 합산. 챗봇 선호 가산 / 비선호 감점 | 개별 상품 선호 → 속성 선호로 변환 |
| ⑤ 스코어링 | **개인 취향 75% + 인기 15% + 신상품 10%**, 안 본 상품엔 발견 가산 | 관심을 안 본 상품까지 확장 |
| ⑥ 16유형 매핑 | 4축 점수 → 16유형 (결정론적) | 복잡한 벡터를 한 마디로 압축 |
| ⑦ 리포트 조립 | payload 확정 → `status: ready` | 결과를 박제해 영구 조회 |
| ⑦′ 문장 생성 (LLM) | 추천 이유·캐릭터 서술 | 같은 유형이어도 "나 맞네"가 나오게 |

- ①~⑦은 순수 연산으로 **10ms 미만**. LLM이 붙는 ③′·⑦′만 **3~4초**.
- 그래서 `/finish`는 **slug만 즉시 반환**하고 사용자는 기다리지 않는다. 같은 `visit_id`로 재호출하면 같은 slug(멱등).
- 채팅 요약 화면을 별도로 만들지 않기로 했으므로, **대화에서 뽑은 내용은 전부 리포트에 녹여 넣는다.**

### 16유형 취향 캐릭터
- 취향 벡터의 핵심 4축 조합 → 16개 캐릭터 중 하나로 결정론적 매핑 (예: `CLBM` "클래식 미니멀리스트").
- 현재 데이터 기준 요약이며 행동이 쌓이면 변할 수 있음.
- confidence가 낮으면 **"탐색 중"** 상태 또는 인접 유형을 함께 제시.
- 각 유형은 대표 키워드 · 선호 소재/색상/실루엣 · 쇼핑 행동 특성 · 추천 상품과 연결.

---

## 7. 행동 이벤트 (분석의 원재료)

프론트가 모아서 **배치로 한 번에** 전송(`POST /events`). 페이지를 떠날 때도 보내 브라우저가 닫혀도 직전 기록이 남게 한다.
저장은 **append-only**, `event_id`로 중복 제거(**멱등**) → 실패하면 그냥 재전송.

| 묶음 | event_type | 역할 |
| --- | --- | --- |
| 매장/방문 | `store_enter` | 관람 시작점. 퍼널의 분모 |
| | `visit_start` | 체류시간 측정 기준 시각 |
| | `visit_end` | 총 체류시간 확정. 리포트 분석 신호 |
| 전시존 | `scene_view` | 어떤 존이 사람을 끌어당기는지(유입) |
| | `scene_dwell` | 관심이 쏠리는 구역. 진열 구성 개선 근거 |
| | `hotspot_click` | 존 → 상품 전환. 진열이 먹혔는지 |
| 상품 | `product_view` | 기본 관심 신호(가장 약한 가중치). 반복 누적이 곧 재조회 횟수 |
| | `product_dwell` | **가장 중요한 신호.** `metadata.dwell_ms` |
| | `product_save` | 찜. 의도가 명확한 강한 신호 |
| | `product_compare` | 구매 직전 고민. 브랜드엔 "자주 비교되는 조합" 데이터 |
| 챗봇 | `chatbot_open` | 정보를 더 원한다는 신호 |
| | `question_submit` | 무엇을 궁금해하는지. 망설임 포착 |
| | `recommendation_impression` | 추천 노출(클릭률의 분모) |
| | `recommendation_click` | 추천이 먹혔는지. 로직 성능 측정 |

> 피팅 관련 이벤트는 **이번 스코프에 없음**(확정).

**`/events` vs `/chat/messages` 역할 분리** — 진열대를 한 번 누르면 보통 둘 다 호출한다.

| | 역할 |
| --- | --- |
| `POST /events` | 분석용 원본 행동 로그. 체류시간·횟수 같은 **수치** |
| `POST /chat/messages` | 화면에 보이는 대화 타임라인 + AI 문맥. **사람이 읽는 메시지** |

---

## 8. 데이터 모델

| 테이블 | 핵심 컬럼 / 역할 |
| --- | --- |
| `products` | `id, name, features`(구조화 피처), `llm_context`(자연어 설명), `price, color, material, images, story, scene_id`, `preset_answers`(price/material/design_intent) |
| `stores` | `id, name, scenes`(전시존 구성). 매장 추가는 코드 수정 없이 row 추가 |
| `users` | `anonymous_uuid, age_band, gender, created_at` — 실명 정보 없음 |
| `visits` | `id, visitor_id, store_id, token, started_at, last_seen_at, ended_at, updated_at` |
| `events` | `event_id, visit_id, event_type, scene_id/product_id, client_timestamp, server_received_at, metadata` (append-only) |
| `chat_logs` | `visit_id, message_id, role, content, scene_id, product_id, created_at` (마스킹·보존기간 대상) |
| `taste_profiles` | `visit_id, vector, axis_scores, character_type, confidence, created_at, updated_at` |
| `reports` | `slug, visit_id, payload(JSON), status(pending/ready/failed), failure_reason, created_at, updated_at` |
| `admin_users` | 브랜드 담당자 계정 (신규) |

- `llm_context`는 **서버 내부 전용**. 응답에 절대 내려주지 않는다.
- **제외 확정**: `fitting_sessions`(피팅), `exit_survey`(퇴장 설문) 테이블은 만들지 않는다.
- 원본(`events`)과 파생(`taste_profiles`, `reports`)을 분리 → 추천 로직을 바꿔도 원본으로 재분석 가능.

**구현 시 확정한 세 가지 (2026-08-12)**
- `taste_profiles`·`reports`는 **방문자를 직접 참조하지 않는다.** `visit`을 거쳐 도달한다. 같은 사실을 두 곳에 두면 어긋날 때 무엇이 진실인지 알 수 없다.
- **타임스탬프 규칙**: append-only 테이블(`events`·`chat_logs`)에는 `updated_at`을 두지 않는다. 상태가 변하는 테이블(`reports`의 pending→ready 등)에만 둔다. `visits`는 `started_at`을 쓰고 `created_at`을 따로 두지 않는다(값이 같다).
- `visits.last_seen_at`은 **이어하기 판정 전용**이며 `/events`·`/chat`에서만 갱신한다. `updated_at`(모든 저장에서 자동 갱신)과 용도가 다르다.

---

## 9. API 계약 요약 (v0.5 기준, 상세는 `API 명세서/`)

### 공통 규약
| 항목 | 내용 |
| --- | --- |
| Base URL | `https://api.oando.example/api/v1` (예시) |
| 포맷 | 요청·응답 `application/json`, UTF-8 |
| 인증 | 사용자 API는 **로그인 없음** — `X-Anonymous-UUID` · `X-Visit-Token` 헤더. `/admin/*`만 `Authorization: Bearer` |
| 매장 | 하나로 고정. 매장 지정 파라미터를 받지 않고 서버가 기본 매장을 붙인다 |
| Stateless | 세션·대화 상태를 서버 메모리에 두지 않음 |
| 멱등성 | `/events`는 `event_id`, `/finish`는 `visit_id` 기준 중복 무시 |
| 시간 | `client_timestamp` + `server_received_at` 둘 다 저장. ISO 8601 / UTC |

에러 포맷 (전 API 통일):
```json
{ "error": { "code": "INVALID_VISIT_TOKEN", "message": "유효하지 않거나 만료된 visit token 입니다.", "detail": null } }
```

| HTTP | code |
| --- | --- |
| 400 | `VALIDATION_ERROR` |
| 401 | `INVALID_VISIT_TOKEN` / `UNAUTHORIZED` |
| 404 | `NOT_FOUND` |
| 409 | `CONFLICT` |
| 429 | `RATE_LIMITED` (LLM 남용·무차별 대입 방지) |
| 500 | `INTERNAL_ERROR` |

### 엔드포인트 (10개)

| 파트 | 기능 | Endpoint | Method |
| --- | --- | --- | --- |
| 공통 | 공통 규약·인증·에러코드·테이블 상세 | — | — |
| Visit | 입장 (Visit 생성) | `/api/v1/enter` | POST |
| Event | 행동 이벤트 수집 (배치) | `/api/v1/events` | POST |
| Product | 상품 상세 조회 | `/api/v1/products/{product_id}` | GET |
| Chat | 채팅 액션 메시지 기록 | `/api/v1/chat/messages` | POST / GET |
| Chat | 컨텍스트 챗봇 (AI ①) | `/api/v1/chat` | POST (SSE) |
| Report | 관람 종료 & 리포트 생성 (AI ②) | `/api/v1/visits/{visit_id}/finish` | POST → 202 |
| Report | 개인 리포트 조회 | `/api/v1/reports/{slug}` | GET |
| Admin | 관리자 로그인 | `/api/v1/admin/auth` | POST |
| Admin | 브랜드 관리자 대시보드 (B2B) | `/api/v1/admin/*` | GET |

주요 응답 형태:
- `POST /enter` → `anonymous_uuid`(신규 발급 시만), `visit_id`, `visit_token`, `store`, `scenes[]`
  - `scenes`는 **좌표·도면이 아니라** "어느 전시존에 어떤 상품이 속하는지"만 알려주는 **논리적 구성**.
  - `scenes`와 상품에 **`no`(번호)** 를 함께 준다 → 챗봇이 "1번 진열대 3번 상품"으로 안내할 수 있어야 하므로.
- `POST /events` → `202 { "accepted": n, "duplicated": n }`
- `GET /products/{id}` → `product_id, name, images, price, attributes, story, scene_id, preset_answers`
- `POST /chat/messages` → `201 { message_id, role, content, created_at }`
- `GET /chat/messages?visit_id=` → `messages[]` + `current_context{scene_id, product_id}` (POST /chat의 기본 문맥)
- `POST /finish` → `202 { "slug": "r_7Ka9xQ", "status": "pending" }` (바디 없음, 추측 어려운 slug)
- `POST /chat` → SSE. `delta` 스트림(위치 안내 문장 포함) → `done`에 `message_id`, `recommendations[{product_id, name, thumbnail, scene_no, product_no, reason}]`
- `GET /reports/{slug}` → `status(pending|ready), character, top_keywords, interested[], recommendations[], confidence, visit_summary`

### 관리자 대시보드 (B2B 수익 모델의 핵심 화면)
| Endpoint | 내용 |
| --- | --- |
| `GET /admin/funnel` | 방문 수, 시간대별 방문자, 평균 체류시간, 전시존 진입률, 상세 전환율, 리포트 완료율 |
| `GET /admin/products` | 상품별 조회·재조회·평균 탐색시간·챗봇 질문 수·찜·추천 클릭 + 자주 비교한 상품 조합 |
| `GET /admin/scenes` | 전시존별 유입·체류·핫스팟 클릭·다음 행동 전환 (진열 개선용) |
| `GET /admin/cohorts` | 색상·소재·실루엣·가격대 취향 분포, 16유형별 선호·퍼널, 연령·성별 관심도 |
| `GET /admin/chat-insights` | 챗봇 질문 주제, 선호 요소, **구매 망설임 요인**, 답변 후 클릭·저장 전환 |

공통 쿼리: `from`, `to`(ISO 8601), `scene_id`, `character_type`
운영: 표본 수가 적거나 confidence가 낮은 지표, 데이터 지연은 화면에 명시. 지표 정의·집계 기준도 화면에서 확인 가능.

---

## 10. 데이터·운영 원칙

1. **원본 불변**: 이벤트는 수정하지 않고(append-only), 분석 파생 데이터는 별도로 생성. 추천 로직을 바꿔도 원본으로 재분석.
2. **워커 안정성**: 재시도 · 지수 백오프 · 멱등 처리 · 실패 보관(DLQ)으로 유실·중복 집계 방지.
3. **설명 가능성**: 모든 추천·유형·리포트에 **근거(reason)와 confidence**를 함께 제공.
4. **개인정보 최소화**: 기본값은 익명 UUID. `chat_logs`는 마스킹·보존기간 적용.
5. **재식별 방지**: 관리자 화면은 개별 사용자 추적 없이 집계·코호트 단위.
6. **stateless API 서버**: 상태는 공유 DB/캐시/큐에 두고 어느 인스턴스가 처리해도 같은 결과.

---

## 11. 스코프 결정 요약

### 이번에 만든다 ✅
- 익명 입장/Visit, 전시존·상품 조회, 행동 이벤트 배치 수집
- 클릭 타임라인 채팅 + 프리셋 답변 + LLM 챗봇(SSE)
- 비동기 리포트 생성(16유형·추천·confidence), slug 영구 조회
- 관리자 로그인 + B2B 집계 대시보드 5종

### 이번엔 안 만든다 ❌ (확정)
| 제외 항목 | 이유 |
| --- | --- |
| 피팅(fitting) 전체 | 스코프 축소 확정. 관련 이벤트·테이블 없음 |
| 퇴장 설문(exit_survey) | 입력 요구가 이탈을 만듦. 연령·성별은 이미 입장 시 수집 |
| 채팅 요약 화면 | 별도 화면 대신 리포트 안에 녹임 |
| 맵/평면도 API | 프론트가 직접 제작. 서버는 논리적 구성만 |
| 메타버스 | 사장된 기술, 유지비만 증가 |
| 매장 다중화 | 매장 하나 고정 (테이블은 확장 가능 구조로만) |
| **재방문 화면(마이페이지) 전체** | **제거 확정.** `GET /me/reports` 없음. 이전 리포트 목록·누적 취향 화면을 만들지 않는다. 완료된 리포트 재확인은 **slug 링크로만** |
| 찜 목록 화면(`GET /saves`) | 만들지 않음. `product_save`는 분석 신호로만 수집 |

### 두 가지를 구분한다 (2026-08-12 확정)

| | 만드나? | 무엇 |
| --- | --- | --- |
| **진행 중 Visit 이어하기** | ✅ **만든다** | 퇴장을 누르기 전에 창을 닫았을 때, 같은 UUID로 다시 들어오면 **그 Visit을 그대로 이어받는다.** 채팅 타임라인·행동 기록이 살아 있고, 퇴장을 누르면 정상적으로 리포트가 생성된다 |
| **재방문 마이페이지** | ❌ 안 만든다 | 이미 끝난 예전 Visit들의 리포트 목록, UUID에 장기 누적된 취향 프로필 화면 |

**이어하기 정책**
- 판정: 같은 `anonymous_uuid`에 `ended_at IS NULL`이고 `last_seen_at`이 **30분 이내**인 Visit이 있으면 이어준다.
- 30분을 넘긴 미종료 Visit은 조회 시점에 만료 처리(`ended_at` 기록, 리포트 생성 안 함)하고 새 Visit을 만든다. 배치 스케줄러는 쓰지 않는다.
- `visit_token`은 DB 토큰이므로 이어할 때 **같은 토큰을 그대로 돌려준다.** (JWT였다면 만료 복구가 불가능했다)
- 한계: UUID는 브라우저 저장소에 있다. 시크릿 모드·저장소 삭제·다른 기기는 복구 불가 — 프론트 안내 문구로 처리.

**`anonymous_uuid`가 하는 일 (3가지)**
1. 로그인 없이 쓰게 하는 편의성
2. **이탈 복구** — 위 이어하기의 열쇠
3. 이벤트 연결 키 + 코호트 집계 단위

**`taste_profiles`** 는 Visit 단위 분석 결과 저장소로만 쓴다. UUID에 취향을 장기 누적해 다음 방문에 보여주는 로직은 없고, 시간 감쇠(time decay)와 "이전 방문 대비 변화"도 스코프 밖이다.

**완료된 리포트의 재열람 경로는 slug 하나.** 그래서 slug는 추측하기 어렵게 발급하고 payload는 박제한다. 프론트 리포트 화면의 "링크 복사/저장" 유도가 유일한 재열람 장치다.

### 아직 미정 ⚠️
| 항목 | 상태 |
| --- | --- |
| 리포트 추천 개수 N | 미확정 |
| 16유형 4축 정의 | 축 이름·경계값 미확정 (매핑은 결정론적이어야 함) |

---

## 12. 팀 · 일정 · 규칙

### 역할
| 담당 | 인원 |
| --- | --- |
| 기획 & 디자인 | 오윤서 |
| 프론트엔드 | 이효령 |
| 백엔드 | 이세호, 이승우 |

### 일정 (2026년 8월)
| 날짜 | 일정 |
| --- | --- |
| 8/3 | 첫 회의 |
| 8/8 | 프론트 회의 |
| 8/11, 8/18 | 정기회의(비대면) |
| 8/12, 8/19 | 정기회의(대면) |
| **8/22** | **개발 마감** |
| 8/28 | 중앙해커톤 |

### Git / 커밋 규칙 (Notion `commit 규칙`)
- 브랜치: `main` 하나만 사용, 바로 push. (작업 브랜치를 쓸 경우 `feat/`, `fix/`, `chore/`, `refactor/` 네이밍)
- 커밋 메시지: `feat: ...` / `fix: ...` / `style: ...` 형식
- `.env` 및 민감정보 커밋 절대 금지. 공유는 `.env.example`에 **키 이름만**.
- 작업 시작 전 `git pull`로 최신화. 충돌은 양쪽 의도를 살려 해결.

### 참고 링크
- Figma UI: `figma.com/design/5LxbFV9v0WQQbjJp98d8FQ/O-O`
- Figma(오앤오레오): `figma.com/design/QHIUu9cCgJCgBCERcM0zTa`
- FigJam BM 보드: `figma.com/board/zoq5UfpF1SPLkkHX6tSc5f/O-O-BM`
- Notion 팀 페이지: `notion.so/1-1fd0ffa59c3d8247a06c010af69a2576`

---

## 13. 백엔드 구현 순서 제안 (DRF)

> 프론트가 막히지 않는 순서 = **스키마 먼저 확정, 로직은 뒤에.**

1. **기반** — `config` settings, `drf-spectacular` Swagger, 공통 예외 핸들러(§9 에러 포맷), CORS
2. **앱 분리** — `stores`(store/scene), `products`, `visits`(user/visit/visit_token 인증 클래스), `events`, `chat`, `reports`, `admin_api`
3. **`POST /enter`** → 프론트가 첫 화면을 그릴 수 있게 최우선. 시드 데이터(전시존·상품·`preset_answers`)도 함께
4. **`GET /products/{id}`** (+ `preset_answers`, `llm_context` 응답 제외 확인)
5. **`POST /events`** — 멱등 저장(`event_id` unique), `server_received_at` 기록
6. **`POST/GET /chat/messages`** — 서버가 문구 생성, `current_context` 계산
7. **`POST /chat`** — SSE 스트리밍 + `chat_logs` 적립 + rate limit
8. **`POST /finish` → `GET /reports/{slug}`** — 먼저 `pending`/목 payload로 계약 확정 후 워커 파이프라인 구현
9. **`/admin/auth` + `/admin/*`** — 집계 쿼리 (모두 `select_related`/`prefetch_related`)

주의 사항(백엔드 관점):
- 모든 사용자 API는 `X-Visit-Token` 검증을 커스텀 인증 클래스 한 곳에 모은다. 뷰에 흩뿌리지 않는다.
- `DEFAULT_PERMISSION_CLASSES`를 잠그고 공개 엔드포인트만 연다(`/enter`는 토큰 없이 진입).
- `llm_context`, `features`는 시리얼라이저 필드 명시로 유출 차단 (`fields = '__all__'` 금지).
- OpenAI 키는 `os.environ`으로만 읽고 `.env.example`에 키 이름만 남긴다.
- 이벤트 집계 쿼리는 상품/전시존 단위 `annotate`로 압축 — N+1은 데모에서 바로 티난다.
