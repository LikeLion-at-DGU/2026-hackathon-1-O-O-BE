# 구현 기록

**무엇을 만들었는지가 아니라 왜 그렇게 만들었는지**를 남기는 문서. 코드와 커밋은 "무엇"을 이미
말하고 있으므로, 여기에는 판단 근거와 버린 대안을 적는다.

기록 형식: `무엇` → `왜 필요한가` → `설계 판단(버린 대안 포함)` → `검증` → `영향`
관련 문서: [`필드_가이드.md`](필드_가이드.md)(프론트용 계약) · [`../tasks/결정사항.md`](../tasks/결정사항.md)(사전 결정) · [`../tasks/마일스톤.md`](../tasks/마일스톤.md)(진행 상황)

---

## 2026-08-12 · 기반 세팅 `4f08265`

### 무엇
Django 5.1 + DRF 프로젝트 골격, 도메인 앱 6개, 모델 전체, 공통 에러 포맷, visit_token 인증, Swagger.

### 왜 필요한가
이후 모든 기능이 이 위에 올라간다. 특히 **인증 방식과 에러 형식은 나중에 바꾸면 프론트가 전부 깨지므로** 첫 커밋에서 확정해야 했다.

### 설계 판단

**SQLite를 쓰되 WAL을 켰다.**
SQLite는 쓰기가 DB 전체를 잠근다. 리포트 워커(백그라운드 스레드)가 도는 동안 `/events` 배치가 들어오면 `database is locked`가 난다. WAL + `timeout=20` + `transaction_mode=IMMEDIATE`로 읽기·쓰기가 서로를 막지 않게 했다.
- 버린 대안: Postgres — 운영이 안전하지만 팀이 SQLite로 결정했다. 대신 아래 두 가지로 보완했다.
- 파생 대응 1: **분석 축 8개를 JSON이 아니라 컬럼으로** 뒀다. SQLite는 JSON 내부 값으로 group by·집계하기가 매우 아프고, `/admin/cohorts`가 색상·소재별 분포를 뽑아야 한다.
- 파생 대응 2: 워커는 계산을 모두 끝낸 뒤 **저장 한 번만** 트랜잭션으로 감싼다(구현 예정).

**`AUTH_USER_MODEL`을 건드리지 않았다.**
사용자는 로그인하지 않고 관리자만 로그인한다. 매장이 하나로 고정이라 브랜드 격리 대상도 없다. 커스텀 유저 모델을 세울 실익이 없어서 Django 기본 `User`를 관리자 계정으로 쓴다.
- 버린 대안: `AdminUser` 커스텀 모델 — 처음엔 명세에 있었지만 "매장 1개 고정"을 반영하면 불필요하다고 판단해 만들지 않았다. `Visitor`(익명)와 이름이 겹치지 않는 부수 효과도 있다.

**Celery를 걷어냈다.**
계약이 요구하는 건 "`/finish`는 즉시 202, 워커가 나중에 `status: ready`"뿐이다. `redis + worker + beat` 3개 프로세스는 얻는 것 없이 데모에서 죽을 지점만 늘린다. `threading.Thread`로 충분하다.

**에러 형식을 한 곳에서 통일했다.**
프론트가 엔드포인트마다 다른 에러 모양을 파싱하지 않게 하려고 커스텀 예외 핸들러를 뒀다. 구현 중 DRF 기본 동작 두 개가 명세와 충돌해서 우회했다.
- DRF 내부 코드(`authentication_failed` 같은 소문자)가 프론트로 새어나가는 문제 → **UPPER_SNAKE code만 신뢰**하고 나머지는 상태코드로 매핑
- `AuthenticationFailed`를 쓰면 `WWW-Authenticate` 헤더가 없을 때 **401을 403으로 강등** → 자체 `Unauthorized` 예외를 만들어 우회
- URL 자체가 안 맞으면 DRF를 타지 않아 HTML이 나가는 문제 → `handler404`/`handler500`도 같은 JSON 형식으로

**`visit_token`은 JWT가 아니라 DB 토큰.**
관람 중 토큰이 만료돼도 **같은 토큰을 그대로 이어 쓸 수 있어야** 한다(이탈 복구). JWT면 만료 복구가 불가능하고 강제 무효화도 안 된다. 요청당 조회 1회가 늘지만 그 대가로 이어하기가 성립한다.

### 검증
`check` 0 issues · 마이그레이션 5개 적용 · `PRAGMA journal_mode` → `wal` 실측 · 401/400/404 응답 형식 실측 · Swagger 200.

### 영향
`AUTH_USER_MODEL`·DB 엔진·에러 형식은 이후 변경하지 않는다. 바꾸면 팀 전체가 깨진다.

---

## 2026-08-12 · 관리자 로그인 `POST /admin/auth` `4f08265`

### 무엇
브랜드 담당자용 JWT 발급. 기본 `User` + simplejwt.

### 왜 필요한가
B2B 대시보드가 수익 모델의 핵심이고, `/admin/*`은 Bearer 인증이 필요하다. 그 토큰을 받는 창구가 없으면 대시보드를 시작할 수 없다. **기반 세팅 단계에서 함께 만든 이유**는 성공/실패 케이스를 실제로 호출해봐야 공통 에러 포맷을 검증할 수 있었기 때문이다.

### 설계 판단
- 사용자 인증과 **완전히 분리**했다. `authentication_classes = []` + `AllowAny`로 두고 자체 검증만 한다. 사용자 API의 visit_token 흐름과 섞이면 구멍이 생긴다.
- `store_ids`는 고정 매장 하나를 그대로 반환한다. 매장 다중화는 스코프 밖이다.
- 무차별 대입 방지로 `10/min` throttle을 붙였다. 인증·권한은 해커톤이라도 깎지 않는다.

### 검증
정상 200 · 잘못된 비밀번호 **401 `UNAUTHORIZED`** · 이메일 형식 오류 400 + 필드별 detail.

---

## 2026-08-12 · 입장 API `POST /enter` + 이어하기 `d611001`

### 무엇
익명 UUID 확인·발급, Visit 생성, 전시존·상품 목록 반환, **진행 중 Visit 이어받기**.

### 왜 필요한가
프론트의 첫 화면이 이 응답 하나로 그려진다. 그리고 익명 UUID를 쓰는 이유의 절반이 여기 있다 —
사용자가 **"관람 끝"을 누르지 않고 창을 닫는 일이 흔하다.** 그때 새 Visit을 만들면 아까 본 상품은 Visit A, 지금 보는 건 Visit B에 쌓여서 **리포트가 반쪽짜리**가 되고 채팅 문맥도 끊긴다.

### 설계 판단

**왕복을 늘리지 않았다.**
`POST /enter` 하나가 판정까지 한다. 진행 중 Visit이 있으면 그걸 그대로 돌려주고 `resumed: true`를 실어준다. 프론트는 그 플래그로 모달을 띄우고, "새로 시작"만 `force_new: true`로 재호출한다.
- 버린 대안: `GET /enter/preview`로 먼저 확인 후 입장 — 왕복이 2회가 되고, 그 사이 상태가 바뀔 수 있다.

**이어할 때 같은 토큰을 그대로 반환한다.**
클라이언트가 저장해 둔 토큰이 계속 유효해야 화면 복구가 매끄럽다. DB 토큰을 고른 결정이 여기서 값을 한다.

**만료를 배치가 아니라 조회 시점에 계산한다.**
`find_resumable()`이 미종료 Visit들을 훑으면서 이어받을 것 하나만 남기고 나머지를 그 자리에서 닫는다(`ended_at` 기록, **리포트는 만들지 않음**). 판정 시점이 입장 한 곳뿐이라 스케줄러를 띄울 이유가 없다.
- 버린 대안: `close_stale_visits` 주기 태스크 — beat 프로세스가 하나 더 필요하고, 상태가 두 곳에서 갈라진다.

**깨진 UUID 헤더는 400이 아니라 새로 발급한다.**
오타·저장소 손상으로 400을 주면 입장 자체가 막혀 서비스를 못 쓴다. 위조로 얻을 이익도 없다(남의 Visit을 이어받으려면 추측 불가한 UUID를 정확히 알아야 한다).

**연령대·성별은 새로 시작할 때만 반영한다.**
이어하기에서 덮어쓰면 이미 답한 사람에게 다시 묻는 화면이 필요해진다. 건너뛰기도 허용하므로 값이 없으면 그대로 둔다.

**`store_enter`·`visit_start` 이벤트는 서버가 만든다.**
프론트도 보내면 퍼널의 분모가 두 배가 된다. 생성 주체를 서버 하나로 못 박았다.

**`scenes`는 좌표·도면을 주지 않는다.**
맵 UI는 프론트 자산이다. 서버는 "어느 전시존에 어떤 상품이 속하는지"만 준다. 대신 `no`(번호)를 함께 줘서 **챗봇이 "3번 진열대 2번 상품"으로 안내**할 수 있게 했다.

### 검증
8개 케이스 실측 — 신규 입장 / 이어하기(**visit_id·token 동일**) / `force_new` / 이전 Visit 만료(리포트 없음) / 31분 초과 시 이어하기 거부 / 잘못된 `age_band` 400 / 깨진 UUID 200 + 재발급 / 빈 바디 200.
쿼리 10개, **전시존·상품 각 1쿼리로 N+1 없음**(`prefetch_related`).

### 영향
프론트 계약 변경 2건 — 요청에 `force_new`, 응답에 `resumed`·`resumed_visit` 추가. 이어하기 모달이 필요하다.

---

## 2026-08-12 · 상품 상세 `GET /products/{id}` + Swagger Authorize `232604b`

### 무엇
상품 상세 조회, Swagger UI의 Authorize 버튼에 `X-Visit-Token` 등록, 토큰 누락 시 401 수정.

### 왜 필요한가
팀원이 직접 API를 눌러보려면 토큰을 넣을 곳이 있어야 한다. Swagger Authorize를 붙이려 했더니
**스키마에 보안 스킴이 안 잡혔다** — `X-Visit-Token`을 소비하는 엔드포인트가 하나도 없었기 때문이다. 그래서 다음 순서였던 상품 상세를 함께 구현했다. 토큰을 테스트할 대상이 필요했다.

### 설계 판단
- `generics.RetrieveAPIView`를 썼다. 표준 조회라 `APIView`로 내려갈 이유가 없다.
- 응답에서 **`llm_context`와 분석 축 8개를 제외**했다. 프론트가 쓸 일이 없고, 챗봇 프롬프트 내용이 노출되면 안 된다. 필드를 명시해서 잠갔다(`fields='__all__'` 금지).
- `preset_answers`를 상세 응답에 함께 내려준다. 프리셋 버튼은 LLM이 아니라 미리 작성한 문구라서, 상품을 열는 순간 이미 손에 있어야 즉시 뜬다.
- `VisitTokenScheme`을 인증 클래스와 **같은 파일에** 뒀다. drf-spectacular는 import된 확장만 등록하는데, 이 모듈은 `DEFAULT_AUTHENTICATION_CLASSES` 때문에 기동 시 항상 로드된다.

### 검증 중 발견한 버그
**토큰 누락이 403 `FORBIDDEN`으로 나갔다.** 명세는 누락·만료·위조를 모두 401 `INVALID_VISIT_TOKEN`으로 정의한다. 원인은 DRF가 인증 정보 없을 때 401을 403으로 강등하는 동작이었다. 퍼미션에서 `False`를 반환하는 대신 `InvalidVisitToken`을 직접 던져 고쳤다.
- 종료된 Visit은 별개다. 토큰 자체는 유효하므로 401이 아니라 403이며, **상품 조회는 막지 않는다**(읽기는 허용).

### 검증
정상 200 · 토큰 누락 401 · 위조 토큰 401 · 없는 상품 404 · **내부 필드 유출 0건**(응답 키 9개 확인).

---

## 2026-08-12 · 시드 커맨드 + 임시 데이터 `d611001`

### 무엇
`manage.py seed_demo` — 전시존 4개 + 상품 8개 적재. `apps/catalog/fixtures/demo.json`.

### 왜 필요한가
크롤링(상품 30개)이 오래 걸린다. 데이터가 없으면 `/enter`부터 검증할 수 없어서 기능 개발이 멈춘다. **계약과 로직을 먼저 확정하고 데이터는 나중에 갈아끼우는 순서**로 가기 위해 임시 데이터를 넣었다.

### 설계 판단
- 상품명에 `[임시]` 접두어를 붙였다. 데모에 실수로 남는 것을 눈으로 잡을 수 있다.
- **8개 축은 전부 실제 값으로 채웠다.** 이름은 가짜여도 축이 진짜여야 나중에 스코어링·16유형 매핑을 테스트할 수 있다.
- `update_or_create`로 멱등 적재. 여러 번 돌려도 중복이 생기지 않는다.
- 적재 직후 **축이 빈 상품을 경고로 알려준다.** 축이 비면 그 상품은 취향 분석에서 그냥 사라지기 때문이다.
- 크롤링이 끝나면 **`demo.json`만 교체**하면 된다. 커맨드는 그대로 쓴다.

---

## 2026-08-12 · 도구 설정: ruff에서 마크다운 제외 `d611001`

### 무엇
`pyproject.toml`의 ruff `exclude`에 `*.md` 추가.

### 왜
`ruff format .`이 **마크다운 안의 Python 코드 블록까지 재포맷**했다. `CLAUDE.md`(팀 규칙)와 `tasks/결정사항.md`의 주석 정렬이 뭉개져서 커밋 diff가 오염됐다. 그대로 두면 매 커밋마다 문서가 노이즈로 바뀐다. 되돌린 뒤 제외 규칙을 넣었다.

---

## 2026-08-12 · 문서: API 필드 가이드 `700a2a0`

### 무엇
[`docs/필드_가이드.md`](필드_가이드.md) — 식별자 3종의 차이, `resumed`와 이어하기, `preset_answers`, 에러 code 표, 분류 축 8개, 앞으로 추가될 계약.

### 왜
Swagger는 **스키마의 진실**이지만 "이 값이 왜 있는지"는 알려주지 않는다. 팀원이 `anonymous_uuid`·`visit_id`·`visit_token`을 헷갈리면 이어하기가 통째로 안 돌아간다. 실제로 `resumed`가 무슨 뜻인지 질문이 나와서, 같은 질문이 반복되지 않게 문서로 고정했다.

역할 분담을 명시했다 — **Swagger는 스키마, 이 문서는 의미.**

---

## 2026-08-12 · 행동 이벤트 수집 `POST /events`

### 무엇
프론트가 모아 보낸 행동 이벤트 배치를 append-only로 저장. 멱등 처리, 체류시간 상한, 참조 검증.

### 왜 필요한가
리포트 분석의 **원재료**가 여기서 들어온다. 이게 없으면 취향 벡터를 만들 재료가 없고 대시보드 지표도 전부 빈다. 매 클릭마다 서버를 치지 않고 모아 보내는 이유는 **브라우저가 닫혀도 직전 기록이 남게** 하려는 것이다(주기 전송 + `beforeunload`).

### 설계 판단

**`accepted`/`duplicated`를 정확히 세려고 저장 전에 한 번 조회한다.**
`bulk_create(ignore_conflicts=True)`는 몇 건이 무시됐는지 알려주지 않는다(SQLite에서는 PK도 안 돌려준다). 그래서 기존 `event_id`를 미리 조회해 계산했다. 조회 1회로 끝나고 배치 크기와 무관하다.
- 버린 대안: 건별 `get_or_create` — 50건이면 쿼리 100개. SQLite 쓰기 잠금까지 겹쳐 최악이다.

**배치 내부 중복도 걸러낸다.**
프론트 재시도 로직이 같은 배치를 이어붙이면 한 요청 안에 같은 `event_id`가 두 번 들어올 수 있다. DB 유니크 제약으로도 막히지만 그러면 `accepted` 수가 틀리게 나온다.

**없는 `scene_id`/`product_id`는 400으로 막았다.**
프론트는 `/enter`가 내려준 목록에서 id를 얻으므로 모르는 id가 오면 코드 버그다. 조용히 NULL로 저장하면 **분석 수치가 눈에 안 보이게 틀어진다.** 어떤 id가 문제인지 `detail`에 담아 프론트가 바로 고칠 수 있게 했다.
- 트레이드오프: 배치 하나가 통째로 거부된다. 다만 이벤트는 멱등 재전송이 전제라 데이터가 유실되지는 않는다.

**서버 전용 타입 3종은 버리고 `ignored`로 알려준다.**
`store_enter`·`visit_start`·`visit_end`는 서버가 만든다. 프론트도 보내면 퍼널의 분모가 두 배가 된다. 400으로 막으면 나머지 정상 이벤트까지 날아가므로, 버리되 **몇 건 버렸는지는 응답에 남겼다.** 조용히 사라지면 프론트가 자기 버그를 못 찾는다.
- 계약 변경: 응답에 `ignored` 필드 추가(기존 `accepted`/`duplicated`는 그대로).

**`dwell_ms`는 서버에서 5분으로 자른다.**
탭을 백그라운드에 두면 30분이 찍힌다. 체류시간은 관심도 계산에서 가장 큰 가중치를 갖는 신호인데, 검증 없이 믿으면 취향 프로필이 한 상품에 끌려간다. 클라이언트 값은 신뢰 경계 밖이다.

**클라이언트가 보낸 `visit_id`를 믿지 않는다.**
토큰이 가리키는 방문과 다르면 403이다. 남의 방문에 이벤트를 심으면 그 사람의 리포트가 오염된다.

**종료된 Visit에는 쌓지 않는다(`IsOpenVisit` → 403).**
퇴장 후 들어오는 이벤트를 받으면 이미 박제된 리포트와 원본이 어긋난다.

### 검증
11개 케이스 실측 — 정상 3건 202 / 재전송 `duplicated:3` / **`dwell_ms` 1800000 → 300000** / 배치 내부 중복+서버 전용 혼합 시 `accepted:1 duplicated:1 ignored:2` / 없는 product 400 + id 표시 / 남의 visit_id 403 / 토큰 없음 401 / 없는 event_type 400 / **50건 배치가 쿼리 7개**(건수 무관) / `last_seen_at` 갱신 / 종료된 Visit 403.

### 영향
`last_seen_at`을 여기서 갱신하므로 **이벤트를 주기적으로 보내는 동안 이어하기 30분 타이머가 계속 연장된다.** 관람 중에 만료되지 않는다는 뜻이다.

---

## 2026-08-12 · fix: `/events`의 `X-Anonymous-UUID` 헤더 누락

### 무엇
`POST /events`가 명세에 필수로 적힌 `X-Anonymous-UUID` 헤더를 **아예 보지 않고 있었다.** 필수 검사와 방문자 일치 검사를 추가했다.

### 왜 놓쳤나
`X-Visit-Token`만으로 Visit → Visitor를 알 수 있어서 기능상 필요가 없다고 판단해 구현하지 않았다. **명세를 다시 대조하기 전까지 드러나지 않았다.** 기능이 동작하는 것과 계약을 지키는 것은 다르다.

### 설계 판단
- 명세대로 **필수**로 만들었다(누락 시 400). 프론트는 명세를 보고 이미 보내도록 만들 것이므로, 서버가 받지 않으면 계약이 조용히 갈라진다.
- 값이 토큰의 방문자와 **다르면 403**이다. 두 값이 어긋나면 클라이언트가 다른 브라우저의 토큰을 들고 있다는 뜻이고, 그대로 저장하면 남의 리포트가 오염된다. 깨진 UUID도 같은 이유로 403이다.
- 헤더 이름 상수를 `api/authentication.py` 한 곳으로 모았다. `/enter`와 `/events`가 같은 헤더를 각자 문자열로 들고 있으면 오타가 난다.
- `POST /enter`는 그대로 둔다. 최초 진입에는 UUID가 없어서 필수로 만들 수 없다(명세도 ❌로 표기).

### 검증
두 헤더 정상 202 · UUID 누락 400 `VALIDATION_ERROR` · 남의 UUID 403 · 깨진 UUID 403 · `/enter`는 UUID 없이 200 유지.

### 남은 명세 차이 (의도적)
- **`product_compare` 이벤트 없음** — 프론트에 비교 UI가 없어 이벤트가 안 들어온다. 같은 Visit에서 두 상품을 60초 내 왕복 조회한 것을 서버가 비교로 추론한다(`tasks/결정사항.md` §3-5)
- **응답에 `ignored` 추가** — 서버 전용 타입을 몇 건 버렸는지 알려준다. 기존 `accepted`/`duplicated`는 그대로
- **403을 쓰는 두 경우** — 명세의 에러 표에는 400·401만 있다. 남의 `visit_id`로 기록 시도, 종료된 Visit에 기록 시도는 명세가 정의하지 않은 상황이라 403으로 정했다
- **`dwell_ms` 서버 상한 5분**, **없는 `scene_id`/`product_id` 400** — 명세 미기재. 원본 데이터 오염을 막기 위한 서버 측 방어

---

## 2026-08-12 · 구현 전수 점검에서 발견한 5건 수정

명세·기획과 코드를 다시 대조하면서 "동작은 하지만 나중에 터지는" 것들을 찾았다.

### ① CORS 허용 헤더가 기본 목록을 덮어쓰고 있었다 (가장 위험)
`CORS_ALLOW_HEADERS`에 4개만 나열해서 django-cors-headers의 기본값(`accept`·`origin`·`x-requested-with`·`x-csrftoken` 등)이 **전부 사라졌다.** 프론트가 붙는 순간 브라우저 preflight가 거부될 수 있었고, 백엔드 단독 테스트(curl)로는 절대 드러나지 않는다. `default_headers`를 펼친 뒤 커스텀 두 개를 더하는 방식으로 고쳤다.

### ② `dwell_ms`에 문자열이 들어와도 저장됐다
`'아무문자열'`이 그대로 `metadata`에 저장됐다. 저장은 성공하고 **나중에 워커가 합산할 때 터진다.** 가장 나쁜 형태의 버그다. 시리얼라이저에서 0 이상 정수만 받도록 막았다. 음수(`-5000`)도 같이 막았다 — 관심도 계산을 왜곡한다.
- 상한(5분) 처리는 서비스에 남겼다. 형식 검증은 입구에서, 정책은 안에서.

### ③ 대상 없는 이벤트가 통과했다
`product_id` 없는 `product_view`가 201로 저장됐다. 저장은 되지만 **분석에서 조용히 사라진다.** 지표가 틀렸는데 아무도 모르는 상태가 된다. 타입별 필수 대상을 시리얼라이저에서 검증한다.
- `scene_*`·`hotspot_click` → `scene_id` 필수
- `product_*`·`recommendation_click` → `product_id` 필수
- `chatbot_open`·`question_submit`·`recommendation_impression`은 대상이 없어도 의미가 있어 그대로 둔다

### ④ 배치 크기 상한이 없었다
프론트 재시도가 겹치면 수만 건이 한 요청으로 올 수 있다. SQLite 쓰기 잠금까지 겹치면 서버가 멈춘다. `EVENT_BATCH_MAX = 200`으로 제한했다.

### ⑤ 시드의 축 검사가 2개만 보고 있었다
`_products_missing_axes()`가 `category`와 `mood`만 확인했다. 이름은 "축 누락 검사"인데 8개 중 2개만 봤다. 크롤링 데이터 30개를 넣을 때 **누락을 못 잡으면 그 상품이 분석에서 사라진다.** 8개 전부 검사하도록 고쳤다.

### 검증
새 케이스 5개(문자열 dwell / 음수 dwell / 대상 없는 product_view / 대상 없는 scene_view / 201건 배치) 전부 400 + 필드별 메시지. 기존 경로 회귀 확인 — `/enter` 200, 이어하기 `resumed:true`, `/products` 200, `/admin/auth` 200, `/django-admin/` 302, `/api/schema/` 200.

---

## 2026-08-12 · 스키마 정리: 중복 필드 4건 제거

### 왜
`last_seen_at`과 `updated_at`의 차이를 설명하다가 `Visit`에 값이 항상 같은 컬럼 두 개가 있는 걸 발견했다. 전체 모델을 훑어 같은 종류를 모두 찾았다. **AI 코드 리뷰가 정확히 이런 걸 지적한다.**

| 제거 | 이유 |
| --- | --- |
| `Visit.created_at` | `started_at`(auto_now_add)과 값이 항상 같다. 명세가 쓰는 이름이 `started_at`이라 그쪽을 남겼다 |
| `TasteProfile.visitor` | `visit.visitor`로 도달한다. 재방문 조회 기능을 없앴으므로 방문자로 직접 찾을 경로도 없다 |
| `Report.visitor` | 같은 이유. **같은 사실을 두 곳에 두면 어긋날 때 무엇이 진실인지 알 수 없다** |
| `ChatLog.updated_at` | 메시지는 쌓이면 고치지 않는다(append-only). `Event`는 같은 이유로 없는데 `ChatLog`만 있어서 규칙이 갈라져 있었다 |

**타임스탬프 규칙을 명시했다**: append-only 테이블(`Event`·`ChatLog`)에는 `updated_at`을 두지 않는다. 상태가 변하는 테이블(`Report`의 pending→ready 등)에만 둔다.

### 함께 정리한 것
- `PresetKey`가 정의만 되고 아무도 안 쓰고 있었다 → `seed_demo`가 **프리셋 3종 누락을 검사**하는 데 쓴다. 크롤링 데이터에서 프리셋이 빠지면 상품 클릭 시 버튼이 비어 보인다
- 아직 참조 0곳인 선언 2개(`CHAT_TIMELINE_LIMIT`, throttle `chat` 레이트) 제거 → 쓰는 시점(F5·F6)에 다시 넣는다

### 검증
마이그레이션 3개 추가 후 `migrate` 성공(기존 파일은 수정하지 않았다) · `makemigrations --check` 통과 · 회귀 확인(`/enter` → `/events` 202 → 이어하기 `products_viewed: 1` → `/products` 200).

---

## 2026-08-12 · 채팅 액션 메시지 기록 `POST·GET /chat/messages`

### 무엇
클릭을 말풍선으로 적립하고 타임라인을 복원한다. 진열대·상품 클릭, 프리셋 열람, 챗봇 첫 인사, `current_context` 계산.

### 왜 필요한가
**이 API가 O&O의 차별점을 지탱한다.** 채팅창이 빈 화면에서 시작하지 않고, 매장을 돌아다닌 행적이 이미 쌓여 있다. 그래서 "채팅으로 대화하기"로 넘어갔을 때 사용자가 **상품명을 말하지 않아도** AI가 어떤 상품 얘기인지 안다.
그리고 이 기록이 리포트 분석(AI ②) ③′단계의 입력이다. 직전까지 `chat_logs`가 0건이라 AI 두 지점 모두 재료가 없었다.

### 설계 판단

**표시 문구를 서버가 만든다** (명세 요구사항).
클라이언트는 `type` + 대상 id만 보낸다. 클라이언트가 보낸 문장은 그대로 AI 프롬프트에 들어가므로 신뢰 경계 밖이다. 브랜드 문구 통제와 표현 일관성도 함께 얻는다.

**직전과 같은 클릭은 쌓지 않고 `200`으로 기존 메시지를 돌려준다.**
같은 상품을 연속으로 누르면 화면이 같은 말풍선으로 도배된다. 다만 **수치는 잃지 않는다** — 조회 횟수는 `POST /events`가 따로 남기고, 프론트는 클릭 한 번에 두 API를 모두 호출한다. 역할 분리가 여기서 값을 한다.
- 중복 판정은 **직전 `user_action`의 문구 비교**로 한다. 액션 타입을 별도 컬럼으로 저장하지 않아도 되고(마이그레이션 불필요), "진열대1 → 상품A → 진열대1"처럼 돌아온 경우는 문구가 달라 정상적으로 쌓인다.
- **프리셋은 중복 제거 대상에서 뺐다.** 같은 걸 다시 물어보는 것 자체가 의미 있는 행동이다.

**프리셋 열람은 말풍선 2개를 만든다.**
누른 버튼(`user_action` "가격") + 미리 작성된 답변(`preset`). 답변만 남기면 타임라인이 "갑자기 가격 얘기가 나오는" 것처럼 읽힌다. 응답으로는 답변 쪽을 돌려준다 — 버튼 라벨은 프론트가 이미 안다.
- 프리셋 문구가 비어 있으면 폴백 문장 + `logger.warning`. 데모 중 화면이 깨지는 것보다 낫고, 누락은 `seed_demo` 검사로도 잡는다.

**챗봇 첫 인사를 서버가 입장 시 적립한다.**
프론트 하드코딩이 아니라 DB에 넣는 이유: `GET /chat/messages` **하나로 화면이 완전히 복원**돼야 한다. 이어하기로 돌아왔을 때도 인사가 제자리에 있다.

**`current_context`는 상품만 클릭해도 `scene_id`를 채운다.**
가장 최근 상품이 속한 존으로 보완한다. 챗봇이 "3번 진열대 2번 상품"처럼 번호로 안내해야 하므로 존 정보가 비면 안 된다.

**읽기와 쓰기의 권한을 다르게 뒀다.**
`POST`는 진행 중 관람만(`IsOpenVisit`), `GET`은 종료된 관람도 허용한다. 퇴장 후 리포트를 기다리는 동안에도 대화를 볼 수 있어야 한다.

**멱등 키를 추가하지 않았다.**
명세에 없고, 이 API는 화면·문맥용이다. 재전송으로 말풍선이 하나 중복돼도 분석 수치는 `/events`가 담당하므로 데이터가 망가지지 않는다. 프론트에 "실패 시 재시도 불필요"를 안내한다.

### 검증
시안 흐름 재현 — 진열대 클릭 201 → 같은 진열대 재클릭 **200(중복 무시)** → 상품 클릭 201 → 프리셋 `price` 201 → 같은 프리셋 다시 201(반복 허용) → 프리셋 `material` 201 → 상품 본 뒤 진열대 재클릭 201(문구가 달라 정상 적립).
복원 결과가 시안과 동일(인사 → 진열대 → 상품 → 가격/답변 → 재질/답변). `current_context` = `{sc_01, p_101}`.
실패 케이스 7개 — 없는 `type` 400 · `scene_id` 누락 400 · 없는 상품 400 · `preset_key` 누락 400 · 잘못된 `preset_key` 400 · 남의 `visit_id` 403 · 토큰 없음 401.
이어하기 후 타임라인 복원 확인(인사 포함) · 종료된 관람 GET 200 / POST 403.

---

## 2026-08-12 · 컨텍스트 챗봇 `POST /chat` (AI ①) + LLM 게이트웨이

### 무엇
OpenAI 스트리밍 응답, 서버가 조립하는 프롬프트, 방문 단위 throttle, 대화 적립.

### 왜 필요한가
프리셋 3종으로 안 풀리는 질문을 받는 통로다. 그리고 **행동만으로는 안 드러나는 망설임을 잡는 유일한 경로**다 — 여기서 나온 대화가 리포트 분석 ③′단계의 재료가 된다.

### 설계 판단

**클라이언트는 문맥을 보내지 않는다.**
서버가 `chat_logs`에서 최근 12개 메시지와 마지막으로 클릭된 상품을 읽어 프롬프트를 만든다. 그래서 "이 상품의 디자이너 의도를 설명해줘"가 상품명 없이 동작한다. `context`를 명시로 보내면 그게 우선한다(명세 요구사항).

**클릭 기록(`user_action`)도 프롬프트에 넣는다.**
"1번 진열대 클릭 → OO 상품 클릭 → 가격" 흐름이 그대로 대화 이력이 되므로 OpenAI 메시지에서는 `user` 역할로 매핑했다. 프리셋 답변은 `assistant`로 넣는다 — 챗봇이 이미 한 말이기 때문이다.

**LLM 접점을 `common/llm.py` 하나로 묶었다.**
앱 코드는 OpenAI SDK를 import하지 않는다. 모델명은 `.env`의 `OPENAI_MODEL`로 빼서 교체 지점을 한 곳으로 만들었고, 키가 없으면 원인을 명시한 예외를 던진다(데모 중 조용히 실패하는 게 최악이다).

**답변은 스트림이 끝난 뒤 한 번에 저장한다.**
조각마다 UPDATE를 치면 SQLite 쓰기 잠금이 스트리밍 도중에 걸린다.

**LLM 실패를 SSE 이벤트로 보낸다.**
스트림이 시작된 뒤에는 HTTP 상태를 바꿀 수 없다. `data: {"error": {...}}`를 흘려보내 프론트가 그 자리에 안내를 띄울 수 있게 했다.

**throttle을 방문 단위로 셌다.**
DRF 기본 `SimpleRateThrottle`은 IP를 쓰는데, 매장 와이파이를 함께 쓰는 손님 전체가 한 사람으로 묶여 서로의 한도를 잡아먹는다. `visit.id`를 키로 바꿨다.

### 검증 중 발견해서 고친 것
첫 라이브 테스트에서 "디자이너 의도를 설명해줘"에 **"제공된 정보에 없습니다"** 라는 답이 나왔다. 프롬프트에 `llm_context`와 분류 축만 넣고 **`story`와 `preset_answers`를 빼먹었기 때문**이다. 정작 그 답이 거기 있었다.
- 상품에 대해 서버가 아는 사실을 전부 넣도록 고쳤다. 특히 **프리셋 문구는 브랜드가 직접 작성한 문장이라 근거로 삼기에 가장 안전하다.**
- 고친 뒤: "1976년 뮌헨의 여행 가방 실루엣을 현대적으로 재해석해 디자인되었습니다" / "코팅 캔버스에 레더 트리밍으로 생활 방수가 가능합니다. 마른 천으로 닦아 주시면 됩니다"
- 가드레일도 확인됐다 — "지금 재고 있어? 할인은 언제 해?" → "수시로 변경될 수 있으니 매장 직원에게 확인해 보세요"

### 검증
라이브 스트리밍 36조각 + `done` 이벤트 · 질문/답변 `chat_logs` 적립 · `X-Accel-Buffering: no` 헤더 · 키 없을 때 `LLM_UNAVAILABLE` 이벤트 · `message` 누락 400 · 501자 400 · 남의 `visit_id` 403 · 토큰 없음 401 · **21번째 요청에서 429 `RATE_LIMITED`**.

---

## 2026-08-13 · Visit·Event·Product·Chat 3회 검토

같은 코드를 세 번 다른 눈으로 읽었다. 1차 명세 대조, 2차 실패 모드·미검증 경로, 3차 클린코드.

### 1차 — 명세 대조: 이상 없음
네 파트의 요청·응답 필드, 상태코드, 에러 코드가 명세와 일치. `/events`의 `ignored`와 `product_compare` 제외는 이미 문서화된 의도적 차이.

### 2차 — 실패 모드: 미검증 경로 3개를 실제로 찔러봤다

**① `context` 명시 지정이 한 번도 테스트되지 않았다** (명세 요구사항: "context를 명시로 보내면 그 값이 우선한다").
- 결과: 정상 동작. `context={product_id: p_301}`을 보내면 클릭 기록(p_101)을 무시하고 p_301로 답한다.
- 우려했던 문제 하나 확인: DRF `Serializer`에는 `context` 프로퍼티가 있어서 같은 이름의 필드가 충돌할 수 있는데, `validated_data`로 접근하므로 문제없었다.
- **다만 없는 `product_id`를 보내면 조용히 무시됐다.** `/events`는 같은 상황을 400으로 막는데 여기만 관대했다 → 400으로 통일했다.

**② ASGI(uvicorn)에서 SSE가 도는지 확인하지 않았다.**
README가 SSE 확인용으로 uvicorn을 권하는데 검증은 `runserver`(WSGI)로만 했다. 스트리밍 제너레이터 안에서 ORM을 쓰기 때문에 ASGI에서 `SynchronousOnlyOperation`이 날 수 있었다.
- 결과: uvicorn에서도 `/enter` → `/chat/messages` → `/chat` 전부 정상, 로그 오류 0건.

**③ 동시 입장 경쟁 조건.**
같은 UUID로 `/enter`가 동시에 여러 번 오면 미종료 Visit이 여러 개 생겨 분석이 쪼개질 수 있다.
- 결과: 3회 동시 요청에서도 Visit은 1개. **`transaction_mode=IMMEDIATE`가 쓰기 트랜잭션을 직렬화**해 준 덕이다. SQLite는 `select_for_update`를 지원하지 않아 다른 방어책이 없었는데, DB 설정이 이미 막아주고 있었다.

**남겨둔 한계**: `ChatLog`·`Event`에는 단조 증가 정렬 키가 없어 `created_at`이 마이크로초까지 같으면 순서가 뒤바뀔 수 있다. 실제로는 INSERT 간격이 마이크로초보다 커서 발생하지 않으며, 컬럼을 추가할 만한 값은 없다고 판단했다.

### 3차 — 클린코드: 5개 수정

| 항목 | 문제 | 수정 |
| --- | --- | --- |
| 보안 규칙 중복 | "클라이언트가 보낸 `visit_id`를 믿지 않는다"가 `chat/views.py`와 `events/views.py`에 **각각 다른 메시지로** 구현돼 있었다 | `api/scoping.py`의 `assert_own_visit()` 하나로 통합 |
| `GET /chat/messages` | `visit_id` 누락 시 403이 나갔다. 필수 파라미터 누락은 400이 맞다 | `TimelineQuerySerializer`로 검증 → 400 |
| `context` 검증 | 없는 상품·전시존을 조용히 무시 | `ContextInputSerializer`에서 존재 검증 → 400 |
| 시리얼라이저 이름 | `ContextSerializer`(출력)와 `ChatContextSerializer`(입력)가 이름만으로 구분되지 않았다 | `CurrentContextSerializer` / `ContextInputSerializer` |
| 잡음 | `summarize()`의 `.values()`는 `aggregate` 앞에서 아무 일도 하지 않았고, `or 0`도 불필요(`Count`는 None을 반환하지 않는다). `_resolve_visit`에 반환 타입 힌트 누락. `current_context()`가 상품의 진열대를 읽으려 쿼리를 한 번 더 냈다 | 각각 제거·추가·`select_related` |

### 검증
회귀 16개 케이스 전부 통과 — Visit 3(입장·이어하기·resumed_visit) · Product 3(조회·404·401) · Event 3(202·남의 visit 403·헤더 누락 400) · Chat 6(적립 201·중복 200·타임라인 200·파라미터 누락 400·context 검증 400·context 정상 200).

---

## 2026-08-13 · 챗봇 트리거 로직 구현 (뮤즈 · 추상→구체)

### 무엇
관찰 → 가설 → Y/N → 분기 루프. 축 계산(`analysis/taste.py`), 추천(`analysis/recommend.py`), 트리거 6종(`chat/triggers.py`), 응답 처리(`chat/answers.py`), 어휘 사전(`chat/taste_map.py`), 손님용 표현(`chat/wording.py`).

### 왜 필요한가
"좋아하는 색이 뭐예요?"는 설문이고, "블랙 위주로 보셨네요, 색은 이쪽이 편하세요?"는 대화다. 매장에서 서 있는 손님에게 타이핑을 요구하지 않고 버튼 한 번으로 취향을 확정받는다. 그리고 **축을 묻지 않고 상품을 묻는다** — 상품 하나에 8축이 붙어 있어 "이 백팩 마음에 드세요?" 한 번이 축 8개를 가져온다.

### 설계 판단

**축값은 누적이 아니라 함수다.** `축값 = f(관심 상품 집합, 회피 데이터)`. 같은 입력에서 같은 결과가 나오므로 저장하지 않고, 방문 중 표시와 퇴장 후 워커 확정이 같은 함수를 돈다. **행동 점수 이중 계산 문제가 구조적으로 사라진다.**

**비율 + 표본 축소 + 진영 격차.** 여러 상품의 공통점이 취향이다(비율). 표본 1개가 1.0을 갖지 못하게 `× n/(n+2)`. 유효성은 값이 아니라 **진영 격차**로 본다 — `black 0.3 · navy 0.3 · pink 0.3`을 값 단위로 보면 "색 취향 없음"이 되는데, 무채색을 두 종류 골랐으니 틀린 판정이다.

**저장은 events에 없는 것만.** `TasteProfile.vector`에 `locks`·`spoken`·`avoided`·`confirmed`·`rejected`·`asked`·예산 카운터·`pending`. JSONField라 **마이그레이션 0**이다. `ActionType`에 3종을 추가했지만 API 입력 enum이라 DB가 안 바뀐다.

**`pending`을 저장한다.** 문서에는 "파생 계산"으로 적었지만 구현에서 불가능했다 — 응답이 왔을 때 그게 어느 축·어느 상품을 물은 가설이었는지 알아야 하는데 `chat_logs`에는 문구만 남는다.

**진영 누락을 기동 시 검사한다.** `analysis.E001` system check. 축 값이 어느 진영에도 없으면 그 값을 고른 손님이 **조용히 반대 진영으로 분류**된다 — 에러 없이 결과만 틀리는 종류다.

### 구현하며 잡은 결함 8개

| # | 결함 | 왜 위험한가 |
| --- | --- | --- |
| 1 | **`cognac`이 진영에 없었다** | 코냑을 많이 본 손님이 "눈에 띄는 색"으로 판정됐다. 위 system check를 만든 계기 |
| 2 | **집합 1개로 4축 전부 유효** | 유효성이 비율만 보므로 상품 하나로 유형이 확정된다 → 최소 집합 크기 3 (lock 면제) |
| 3 | **2군이 도달 불가 코드였다** | 왕복 조회는 재조회를 포함하므로 1군이 항상 먼저 먹는다 → 2군을 앞으로 |
| 4 | **빠른 훑기가 절대 발동 못 함** | 발동 조건을 "체류 5초+ 조회 4개"로 뒀는데 훑는 손님은 정의상 0개다 → 조회 총수로 |
| 5 | **축 확인 조건 ②·④가 상호 모순** | 5개 중 3개(60%)면 나머지 2개라 항상 탈락 → 실질 임계가 80%가 되어 ②가 무의미 → 2등 값 기준 |
| 6 | **축 확인 조건 ⑤가 발동 불가** | 축값 0.7 요구인데 shrinkage 때문에 집합 5개 전부 같은 값이어야 0.71 → `confirmed` 상품을 직접 확인 |
| 7 | **부정 표현이 뒤집혔다** | `"검은색 말고"`가 분리 기호 `"고 "`에 쪼개져 부정어가 사라짐 → 보호 마커 후 분리. 그 뒤 `"A는 좋고 B는 싫어요"`에서 절 분리가 안 돼 문장 전체가 부정이 되는 문제도 나와 두 단계로 분리 |
| 8 | **같은 축이면 부정을 지웠다** | `"미니멀은 좋고 볼드는 싫다"`는 정합한 진술인데 축 단위로 지워 부정이 사라졌다 → 같은 축 + 같은 값일 때만 |

7·8은 **정반대 해석**이 나가는 결함이었다. 사전 매칭은 LLM 없이 도는 경로라 여기가 틀리면 조용히 반대 추천이 나간다.

### 검증
어휘 사전 7케이스(부정·병렬·다중값) · 축 계산 4케이스(집합 크기별 유효성·lock·shrinkage) · 트리거 5시나리오 · 응답 4분기(맞아요/아니에요/2택/구제) · E2E 상품 확인 → 축 확인 → lock(완성도 0.75) · `analysis.E001`이 실제로 걸리는지 역검증.

**테스트 실수 하나 기록**: `events` 리스트를 변수에 담아 두 번 보내면 `event_id`가 같아 전부 `duplicated`로 걸러진다. 방문이 달라도 `event_id`는 전역 PK라 멱등이 적용된다 — 검증할 때 매번 새 UUID를 만들어야 한다.

### 남은 것
- **4군 회피형은 임시 시드(존당 상품 2개)로 발동 불가**하다. 같은 존에 같은 축 값 상품이 2개 이상 있어야 한다 → 상품 30개 들어온 뒤 검증
- 격차 임계 `0.25`·shrinkage `2`·`MIN_INTEREST_FOR_AXIS 3`은 실데이터로 재조정

---

## 2026-08-13 · fix: `/chat` 500 (multi-value 회귀)

### 무엇
`_reading()`이 리스트를 `say()`에 넘겨 `TypeError: unhashable type: 'list'`로 500이 났다.

### 왜 놓쳤나 — 두 겹의 실수

**① 수정이 부분만 적용됐다.** 어휘 추출을 `dict[str, str]` → `dict[str, list[str]]`로 바꿀 때 저장부(`spoken`·`avoided`)는 고쳤지만 반환 딕트와 `_reading()`은 그대로였다. 문자열 치환으로 고치면서 대상 문장이 포매터에 의해 미세하게 바뀌어 있었고(docstring 앞 공백), **치환이 실패했는데 실패를 확인하지 않았다.**

**② 상위 호출 경로를 재검증하지 않았다.** 수정 후 `extract()` 단위 테스트만 3케이스 돌렸다. `/chat` API는 그 수정 **이전**에 검증한 것이 마지막이었다. 함수 시그니처를 바꿨으면 그 함수를 부르는 경로를 다시 태워야 한다.

`test Client`로는 재현되지 않았다 — 그 경로를 아예 호출하지 않았기 때문이다. **실서버 스모크가 유일한 발견 지점**이었다.

### 재발 방지
- 시그니처를 바꾸면 그 함수의 호출부를 `grep`으로 전수 확인한다 (`preferred.values()` 같은 패턴)
- 문자열 치환 후에는 **바뀐 결과를 눈으로 확인**한다. 치환 실패는 조용하다
- 계약이 바뀐 엔드포인트는 **실서버로** 다시 태운다. `test Client` 통과가 실서버 통과를 뜻하지 않는다(스트리밍 응답은 특히)

### 검증
실서버에서 발화 4종 — 단일 축 · 다중 값 부정("빨강도 핑크도 싫어") · 병렬 긍정+부정 · 소재 문의. 전부 200, traceback 0건, `extracted`·`profile_completion`·추천 정상.

---

## 2026-08-18 · 배포 준비 (가비아 VM)

### 무엇
`deploy/` 디렉터리(nginx·systemd·백업·가이드)와 배포용 설정을 추가했다. 서버를 받기 전에 미리 만들어, 받은 날 30분 안에 올릴 수 있게 하는 것이 목적이다.

### 왜 HTTPS가 협상 불가인가
프론트가 Netlify(HTTPS)에 올라간다. HTTPS 페이지에서 HTTP API를 부르면 브라우저가 mixed content로 차단한다. 즉 **인증서가 없으면 연동 자체가 0%**다.

도메인 구매 없이 인증서를 받기 위해 `sslip.io`를 쓴다(IP를 도메인처럼 해석해주는 공개 DNS). Let's Encrypt의 HTTP-01 검증이 통과한다.

**버린 대안**: Netlify의 `/api/*` 프록시 리다이렉트. 같은 오리진이 되어 CORS가 사라지는 매력이 있지만 **Netlify 프록시는 응답을 버퍼링하고 타임아웃이 짧다** — 챗봇 SSE가 죽는다.

### 발견한 블로커 2건
- **`STATIC_ROOT` 미설정** → `DEBUG=False`에서 `collectstatic` 실패 → `django-admin` CSS 깨짐. 상품 30개를 admin으로 입력할 계획이라 실제 장애다.
- **미디어 미서빙** → `config/urls.py`가 `DEBUG`일 때만 미디어를 붙인다. 배포에서는 nginx `location /media/`가 담당한다.

whitenoise를 쓰지 않은 이유: nginx가 어차피 앞에 서므로 static을 nginx에서 주면 된다. 의존성을 하나 더 늘릴 이유가 없다(미디어는 whitenoise가 다루지도 않는다).

### 프록시 뒤에서 필요한 설정
- `SECURE_PROXY_SSL_HEADER` — nginx가 TLS를 끊으므로 없으면 Django가 자기를 http로 착각해 Swagger 서버 주소와 미디어 URL이 http로 나간다(= mixed content).
- `CSRF_TRUSTED_ORIGINS` — https `django-admin` 로그인에 필요.
- `SESSION_COOKIE_SECURE`/`CSRF_COOKIE_SECURE`는 `not DEBUG`에 묶었다. 상수 `True`로 두면 로컬 http admin 로그인이 막힌다.
- `CORS_ALLOWED_ORIGIN_REGEXES` — Netlify PR 미리보기는 `deploy-preview-3--사이트.netlify.app`처럼 주소가 매번 다르다. 고정 목록만으로는 미리보기에서 API가 전부 막힌다.

### nginx에서 지우면 안 되는 것
`proxy_buffering off` + `proxy_cache off`. 켜져 있으면 nginx가 SSE를 다 모았다가 한 번에 보내 스트리밍이 무의미해진다. 코드의 `X-Accel-Buffering: no`만 믿으면 안 된다.

### 워커 1개인 이유
리포트 생성이 `threading.Thread`이고 DB가 SQLite다. SQLite는 쓰기가 DB 전체를 잠그므로 프로세스를 늘리면 쓰기 경합만 커진다. 2vCore·데모 트래픽에는 1개로 충분하다.

### 일정 리스크
서버 생성 가능일 **8/18 = 개발 마감일**, 기술지원 종료 **8/21 18:00**, 삭제 **8/28 23:59(중커톤 당일)**. 마감일에 배포까지 몰면 둘 다 터진다 → 8/18 오전에 뼈대만 먼저 올려 프론트에 주소를 넘기고, 이후 코드는 `git pull`로 갱신한다. 데모 직후 `deploy/backup.sh`로 DB·미디어·`.env`를 받아둔다(서버는 통보 없이 삭제된다).

### 검증
`DEBUG=False` 실서버 기동 — `collectstatic` 163개 성공(`staticfiles/admin/css/base.css` 확인), Swagger 200, `/enter` 200, `django-admin` 302. CORS 정규식은 `deploy-preview-3--oando.netlify.app`에 허용 헤더가 나가고 `evil.example.com`에는 나가지 않는 것까지 확인.

---

## 2026-08-18 · 화보 대응 ①② — 토큰 규약 변경 + `/enter` 확장

### 무엇
v0.6 화보 명세의 착수 순서 1·2번. 화보 API를 만들기 전에 **전 구간이 선행으로 걸려 있던 두 가지**를 먼저 걷어냈다.

### ① 토큰은 왜 "퇴장 시 만료"를 버렸나

기존 만료 조건은 두 개였다 — `/finish` 호출 즉시, 그리고 진입 후 일정 시간. 그런데 **화보와 재생성이 전부 `/finish` 이후에 일어난다.** [다시 돌리기]는 화보가 완성된 그 화면의 버튼인데, 기존 규약대로면 그 시점에 토큰이 이미 죽어 있다.

만료 조건을 **진입 후 3시간 하나로** 줄였다. ①이 하던 "리포트 박제 보호"는 `/events` 타입 화이트리스트가 전담한다 — 같은 일을 두 군데서 막고 있었고, 그중 토큰 쪽이 화보를 방해했다.

만료 판정은 인증 시점에 한다(`VisitTokenAuthentication`). 배치를 기다리지 않으므로 사용자 입장에서는 정확히 3시간이다.

**기준 시각을 `last_seen_at`에서 `started_at`으로 바꿨다.** 3시간은 "관람 20분~1시간 + 화보 + 재생성 3회"를 담는 **총 예산**이지 유휴 시간이 아니다. 마지막 활동 기준으로 두면 오래 머무는 사람의 토큰이 무한히 연장된다.

`is_open`(관람 중인가)과 `is_expired`(토큰이 죽었나)를 분리한 것이 이번 변경의 핵심이다. 이전에는 한 값이 두 질문에 답하고 있었다.

### `/events`가 종료된 방문도 받게 된 이유

퍼미션을 `IsOpenVisit` → `IsVisitAuthenticated`로 낮췄다. 화보 이벤트를 받아야 하는 것이 직접적인 이유지만, 원래도 결함이었다 — `/finish` 뒤에 클라이언트가 마지막 배치를 보내면 그대로 401이 나서 **종료 직전 구간이 통째로 유실**된다. 전환 분석에서 가장 중요한 구간이다.

대신 타입으로 가른다. 종료 후에는 `lookbook_*`·`photo_*`만 저장하고 관람 이벤트는 `ignored`로 버린다. 리포트는 `/finish` 시점에 박제되므로 뒤늦게 온 상품 조회는 반영될 수 없고, 그대로 저장하면 원본과 리포트가 서로 다른 말을 하게 된다.

### 배치 상한을 400에서 `rejected`로 바꾼 이유

기존은 200건 초과 시 400이었다. 매장 와이파이가 끊겼다 복구되면 밀린 버퍼가 한 번에 몰리는데, 그때 통째로 거절하면 **유효한 기록까지 같이 날아가고 클라이언트에도 서버에도 흔적이 남지 않는다.**

앞에서부터 200건을 저장하고 나머지 수를 `rejected`로 알려준다. 순서가 보존되므로 클라이언트는 뒤쪽 `rejected`건만 다시 보내면 된다. 메모리 보호용 절대 상한(1000건)은 400으로 남겼다.

`ignored`와 `rejected`를 나눈 기준은 **클라이언트가 재전송해야 하는가**다. 하나로 합치면 프론트가 판단할 수 없다.

### ② `/enter` — 뮤즈 번호와 응답 필드

`muse_no`는 매장 카운터를 올려 발급한다. SQLite에는 `select_for_update`가 없어 `F()` 갱신에 기대는데, 연결이 `transaction_mode=IMMEDIATE`라 트랜잭션 시작 시점에 쓰기 락을 잡는다. 동시 입장 3건에서 N.003/004/005가 나오는 것을 확인했다. 이어하기에서는 재발급하지 않는다 — 뒤로가기 한 번에 번호가 올라가면 안 된다.

`resumed` → `is_resumed`로 이름을 바꾸고 `is_new`를 추가했다. **프론트가 아직 붙이지 않은 시점이라 지금 바꾼다.** 명세 본문 여러 곳이 이 이름을 쓰는데 응답 정의에만 빠져 있어서, 스펙만 보고 구현하면 재진입 분기를 못 만든다.

`anonymous_uuid`를 항상 내려주도록 바꿨다. 신규 발급일 때만 주면, 클라이언트가 보낸 uuid가 죽은 값이라 서버가 새로 발급한 경우 클라이언트는 자기 uuid가 유효하다고 오해하고 **죽은 값을 영원히 계속 보낸다.**

### `age_band`·`gender`를 Visitor → Visit으로 옮긴 이유

같은 사람이 다음 방문에서 다르게 답할 수 있고, 코호트 집계 단위가 방문이다. Visitor에 두면 **마지막 입력이 과거 방문의 집계까지 소급해서 바꾼다.** 명세의 근거를 그대로 따랐다.

### `is_auto_closed`

방치돼서 서버가 닫은 방문과 사용자가 "새로 시작"을 눌러 닫힌 방문은 다르다. 전자만 `True`이고, 평균 체류시간·리포트 완료율의 분모에서 빠진다.

### 검증
실서버 기준. 입장 N.001 → 이어하기에서 번호 유지 → 동시 3건 N.003/004/005 중복 없음. 종료 후 `product_view`는 `ignored`, `lookbook_share`·`photo_capture`는 저장, 섞은 3건은 화보 1건만 통과. `/chat/messages`는 종료 후 403 유지. 250건 배치 → `accepted 200 / rejected 50`. 3시간 1분 경과 토큰 → 401. 2시간 경과는 정상 이어하기(기존 30분 로직이면 만료됐을 건). `force_new`는 `is_auto_closed=False`. 챗봇 스트리밍 회귀 통과(`_describe_visitor`가 Visit을 읽도록 바뀐 경로).

### 남은 것
- `/finish`가 아직 없어 이번 검증은 `ended_at`을 직접 채워서 했다. 리포트 파이프라인 구현 시 재확인
- 화보 5종은 `report.status == ready`를 전제하므로 리포트 파이프라인이 다음 순서

---

## 2026-08-19 · 챗봇 가드레일 프롬프트 (범위 이탈 · 조작 대응)

### 무엇
`GUARDRAIL_PROMPT`를 추가하고 손님 질문 **바로 앞**에 넣었다. 시스템 프롬프트와 질문 사이가 멀면 앞선 지시가 흐려지고, 조작 문장은 대개 이번 질문 안에 들어온다.

### 범위 정책 — 거절이 아니라 전환, 그리고 3층
"완곡 거절" 하나로 통일하거나 "간단히 답하고 문장 붙이기"로 통일하면 양쪽 다 손해다.

- **1층 매장/상품** → 정상 답변
- **2층 매장으로 이어짐**(스타일·상황·색 조합·선물) → **답한다.** "여행 갈 때 뭐 들고 가?"는 `use_case=travel`을 확정할 수 있는 최고의 질문이다. 거절하면 취향 데이터를 스스로 버린다
- **3층 완전 무관**(시사·의학·법률·계산·코드·타 브랜드 평가) → 답을 시도하지 않고 한 문장으로 전환

용어를 "거절"에서 "전환"으로 바꾼 이유: 큐레이터 페르소나에서 "죄송하지만 답변할 수 없습니다"는 콜센터 말투다. 명품 매장 큐레이터는 거절하지 않고 화제를 옮긴다. 그래서 거절 문구를 **명시적으로 금지**하고 O/X 예시를 넣었다.

### 인젝션 방어 — 프롬프트는 마지막 층이다
이미 프롬프트보다 강한 방어가 셋 있었다. 추천 후보를 서버가 고정(목록 밖 상품 추천이 구조적으로 불가), `llm_context`에 재고·할인 없음(지어낼 재료가 없음), 입력 500자·출력 400토큰(긴 탈선이 잘림).

그 위에 얹은 것 중 실제로 효과가 컸던 건 **출력 형식 잠금**이다. "시·표·목록·코드·JSON으로 답하지 않는다"를 넣자 형식 우회가 통째로 막혔다. 규칙 내용을 직접 물으면 막히지만 "시로 번역해줘"로는 새는 문제가 있었는데, 내용을 지키라고 더 강하게 말하는 대신 **형식을 잠그는 쪽이 통했다.**

그리고 조작 시도에 **설명하지 않게** 했다. "저는 시스템 프롬프트를 공개할 수 없습니다"라고 답하면 공격자에게 힌트를 주고 대화가 그쪽으로 끌려간다.

### 발견한 진짜 결함 — 프롬프트가 아니라 데이터 부재
1차 검증에서 **구찌 가방·버버리 트렌치코트·없는 5번 진열대**가 나왔다. 프롬프트는 "아래 추천 후보에 있는 상품과 번호만 쓰라"고 말하는데, 확인해 보니 **후보 블록이 아예 없었다.**

- 상품 클릭 없이 챗봇을 먼저 열면 `current_context`가 비고 `suggest()`도 빈 배열이다
- 가리킬 목록이 없는 상태에서 "목록에만 있는 것을 쓰라"는 지시는 무의미하다. 모델은 빈칸을 사전지식으로 채운다

**프롬프트 문구를 고치는 대신 데이터를 넣었다.**
- `_describe_store()` — 매장 이름·진열대 번호와 이름·취급 분류를 DB에서 읽어 항상 넣는다. "위에 없는 브랜드·상품·진열대 번호는 이 매장에 없습니다"로 닫는다
- `NO_CANDIDATE_NOTE` — 후보와 상품 문맥이 모두 없을 때만 넣는다

`NO_CANDIDATE_NOTE`의 첫 버전은 **매장 질문을 범위 밖으로 만들어버렸다.** "특정 상품을 지목하지 마세요"만 썼더니 모델이 "답할 수 없음"으로 해석해 소재 질문에 전환 문구를 냈다. "이건 답할 수 없는 질문이라는 뜻이 아니다 / 되묻되 전환 문구를 쓰지 말라"를 명시해 고쳤다.

### N+1
`_describe_store()`가 진열대마다 상품을 다시 조회해 프롬프트 조립에 15쿼리가 들었다. `prefetch_related("products")`로 12쿼리.

### 검증
실서버 + 실제 LLM으로 13발화. 상품 질문(문맥 유/무) · 상황 질문 · 무관 질문 2종 · 인젝션 2종 · 확약 유도 · 타 브랜드 비교 · 감정 · 형식 조작 4종(시·JSON·표·역할변경).

- 다른 브랜드 상품 창작 **0건** (1차에서는 3건)
- 없는 진열대 번호 **0건** (1차에서는 2건)
- 형식 조작 4종 전부 2~3문장 산문 유지
- 감정 발화에 상품·진열대·쇼핑 권유 없음
- 문맥 있는 상품 질문은 실제 `preset_answers` 근거로 정확히 답함

### 남은 것
- 전환 문구가 아직 "그 요청은 제 범위를 벗어나네요" 정도로 약하게 거절 느낌이 남는다. 유출·형식 이탈은 없어 기능적으로는 안전하다. 카피 톤은 기획 확인이 필요하다
- 상품 30개가 들어오면 `_describe_store()`의 취급 분류가 길어질 수 있다. 토큰을 재확인할 것

---

## 2026-08-19 · 실상품 63개 반영 후 발견한 2건

### 배경
`fixtures/demo.json`이 임시 8개에서 **실상품 63개(7개 진열대)**로 교체됐다. Django fixture 형식이 아니라 `seed_demo`가 읽는 자체 형식이고, `scripts/xlsx_to_demo.py`가 만든다. 데이터가 바뀌자 코드의 빈틈 두 개가 드러났다.

### ① 챗봇이 DB에 있는 답을 못 했다

임시 시드는 `llm_context`와 `preset_answers`가 채워져 있었다. 실데이터는 **둘 다 63/63 비어 있고**, 대신 `attributes`에 실제 값이 들어왔다.

```
attributes: {"color": "lotus pink", "material": "스페인산 엠보스드 레더",
             "dimensions": "약 11 x 25 x 18cm", "size": "S"}
```

그런데 `_describe()`가 `attributes`를 프롬프트에 넣지 않았다. 결과:

- "크기 얼마예요?" → **"매장 직원에게 문의해 주세요"** (DB에 있는데)
- "소재가 뭐야?" → "그레인 레더" (분석 축의 뭉뚱그린 분류. 실제는 "스페인산 엠보스드 레더")
- "무슨 색이야?" → "핑크색" (실제는 "lotus pink")

`attributes`를 프롬프트에 추가해 셋 다 정확해졌다. 분석 축은 집계용이라 값이 거칠고, **사람이 물어보는 구체적인 사실은 `attributes`에만 있다.**

### ② 빈 축이 취향 벡터를 오염시켰다

실데이터는 4~7단(악세서리·의류)의 분류 축이 비어 있다 — **33/63(52%)**. `_ratios()`가 빈 문자열을 하나의 값으로 셌다.

```
color: {'': 0.667}
```

`''`는 어느 진영에도 없어 **유효성 판정에는 영향이 없었다**(그래서 조용했다). 하지만 `TasteProfile.vector`에 그대로 저장되고, 최빈값이 되면 프롬프트와 리포트 문구로 샐 수 있었다.

빈 값을 세지 않도록 고쳤다. **분모는 전체 관심 상품 수로 유지했다** — 근거가 적을수록 비율이 낮게 나와 판정이 보류되는 쪽이 이 프로젝트의 기존 태도와 맞는다. 분모까지 "값이 있는 상품 수"로 바꾸면 상품 하나로 축이 확정되는 역효과가 난다.

### 테스트에서 한 실수
`INTEREST_DWELL_MS`가 20초인데 9초짜리 체류로 검증해 "축이 전부 무효"로 보였다. 코드 결함으로 오인할 뻔했다. **임계값을 확인하고 테스트 입력을 정하는 것**이 먼저다.

### 남은 데이터 구멍 (코드가 아니라 데이터 작업)
| 항목 | 상태 | 영향 |
| --- | --- | --- |
| 분석 축 8개 | 33/63 비어 있음 | 그 상품은 취향 분석에서 빠진다 |
| `preset_answers` | 63/63 값이 빈 문자열 | 상품 클릭 시 프리셋 버튼 3개가 빈칸 |
| `llm_context` | 63/63 비어 있음 | 챗봇 근거가 `attributes`·`story`뿐 |
| `cutout_url` | 63/63 비어 있음 | 화보 합성 품질 (명세가 "데모 품질을 가장 좌우"라고 지목) |

### 검증
`migrate` → `seed_demo`(63개 적재) → 실서버 + 실LLM으로 상품 문답 3종. 58개 테스트 통과.

---

## 실상품 60개 적재 · 업로드 백엔드 · 화보 실생성 (2026-08-19)

### 왜 presign URL이 uploads.invalid였나

버그가 아니라 `STORAGE_BACKEND=dev`의 설계된 동작이다. `.invalid`는 RFC 2606이 "절대 존재하지 않는다"고 못박은 예약 도메인이고, **받는 척하면 업로드가 됐다고 착각하게 되므로** 일부러 죽은 주소를 쓴다.

문제는 버킷이 없다는 것이었다. `local` 백엔드를 추가해 Django가 PUT을 직접 받는다. **presign 응답 형식은 s3와 완전히 같게 유지했다** — 프론트가 이미 presign→PUT으로 구현을 마쳤고, 나중에 R2가 생겨도 `.env` 한 줄만 바꾸면 되어야 한다.

버린 대안: multipart 직접 업로드. 프론트 재작업이 필요하고, 원 설계가 피하려던 "사진 바이트가 Django를 지나간다"로 되돌아간다.

**사진은 `MEDIA_ROOT`가 아니라 `uploads/`에 쓴다.** nginx가 `/media/`를 통째로 공개 서빙해서, 거기 두면 명세의 `photos/ 비공개, 워커만 접근`이 깨진다. 얼굴 사진이 키만 알면 열리는 파일이 된다.

서명은 붙이지 않기로 했다(사용자 판단). 그래서 **키 형식 정규식이 유일한 방어선**이다 — `../`나 임의 키로 쓰는 시도가 거기서 끊긴다.

### Redis를 쓰는 이유는 파일에 적용되지 않는다

"폴링 상태 때문에 Redis를 쓰니 파일도 공유가 안 될 것"이라는 추론이 있었으나, Redis가 필요한 이유는 **LocMem이 프로세스별 메모리**이기 때문이다. 디스크는 같은 VM의 모든 gunicorn 워커가 공유하고, 화보 워커는 애초에 같은 프로세스 안의 스레드다. 단일 VM에서 `local` 백엔드는 동작한다.

### 화보 생성이 "안 되던" 진짜 이유

DB 저장도 AI 호출도 고장난 게 아니었다. **이미지 생성 코드가 레포에 한 줄도 없었다.** `LOOKBOOK_FAKE_AI=True`가 8초 자고 고정 URL을 돌려주고 있었고, 그 `placeholder.png`조차 존재하지 않아 완료 화면에서 이미지가 깨졌다.

`_generate()`를 실구현했다. `openai` SDK를 1.59.6 → 3.3.0으로 올려야 했다 — 구 버전 `images.edit`는 이미지 1장 + 정사각형만 받아 **레퍼런스를 붙일 수 없다**. 챗봇의 `chat.completions` 호출부는 그대로 동작함을 확인했다.

마스크 규약이 이 작업의 함정이다. OpenAI는 **투명한 영역을 고쳐 그린다.** 인물을 보존하려면 인물이 불투명해야 하므로, 프론트(MediaPipe) 실루엣의 밝기를 알파로 옮긴다. 뒤집히면 얼굴만 새로 그려진다 — 극성이 반대로 올 경우를 대비해 `LOOKBOOK_MASK_INVERT`를 남겼다.

레퍼런스가 없으면 **레퍼런스 문장을 아예 넣지 않는다.** "레퍼런스를 따르라"고 써놓고 그림을 안 주면 모델이 없는 것을 상상해 구도가 튄다. 재생성은 구도·자세만 흔들고 무드는 `mood_payload`의 박제값이라 유지된다.

### CSV 60개 → demo.json

CSV에는 전시존 정보도 분석 축도 없다. 별도로 받은 "60개 목록" PDF가 `전시존-상품` 번호를 갖고 있었고, **CSV 행 순서와 1:1로 맞는 것을 8개 항목(ECONYL 소재, della robbia blue, 38/40 IT 사이즈, cognac 3건)으로 교차 검증**한 뒤 그대로 썼다. `cutout_image`가 `1~60-Photoroom.png` 순번인 것도 같은 순서를 가리킨다.

분석 축 8개는 이름·소재·가격에서 규칙으로 뽑는다. 규칙을 `import_products_csv` 한 곳에 모은 이유는 **"왜 이 상품이 미니멀인가"를 나중에 되짚기 위해서다.**

여기서 한국어 부분문자열 함정을 밟았다. `"카드" in name`으로 지갑을 판정했더니 **"로레토스 자카드 스카프"가 지갑이 됐다.** 낱말을 통째로 보도록 고쳤다.

색상은 CSV에 12종인데 축은 9종이라 가장 가까운 값으로 접는다(blue→navy, gold→metallic). 원본 색상명은 `attributes.color`에 그대로 남긴다 — 집계용 축은 거칠어도 되지만 **챗봇이 "무슨 색이야?"에 답할 때 필요한 건 "lotus pink"** 라는 앞선 교훈 그대로다. green(khaki moss) 1건만 마땅한 값이 없어 beige로 접었다.

id는 기존 DB 체계(`p_{전시존}{순번}`)를 그대로 따랐다. 처음에 전역 순번(`p_101~p_160`)으로 매겼다가 **기존 행을 갱신하지 못하고 51개가 새로 생겼다.** 로컬에서만 벌어진 일이라 정리했지만, 운영에서 그랬으면 진열대마다 옛 상품이 섞여 보였을 것이다.

### 이전 기록의 "남은 데이터 구멍" 결산

| 항목 | 이전 | 지금 |
| --- | --- | --- |
| 분석 축 8개 | 33/63 비어 있음 | **0/60** |
| `preset_answers` | 63/63 빈 문자열 | **0/60** (가격·소재·디자인 의도를 CSV에서 생성) |
| `llm_context` | 63/63 비어 있음 | **0/60** (요약·설명·소재·특징을 문단으로) |
| `cutout_url` | 63/63 비어 있음 | **0/60** |

남성의류가 9→6으로 줄어 `p_607~609`가 고아로 남는다. 운영 DB에서 지워야 6번 진열대에 옛 상품이 안 보인다.

### 검증
`import_products_csv` → `seed_demo`(60개) → `JSON: 60 DB: 60 누락: [] 추가: []`. 축 값이 전부 모델 선택지 안에 있음을 확인. 76개 테스트 통과, ruff·`manage.py check`·`makemigrations --check` 통과.

### 남은 블로커
1. **OpenAI 키가 401이다.** 실생성은 키 교체 전까지 무조건 실패한다.
2. 서버 `.env`의 `STORAGE_BACKEND=dev`, `LOOKBOOK_FAKE_AI=True`를 바꿔야 한다.
3. `Composition.reference_url` 4행이 비어 있다. django-admin에서 채우면 배포 없이 반영된다.
4. **Photoroom PNG 60개를 `/srv/oando/media/products/`에 올려야 한다.** 상품 이미지와 컷아웃이 둘 다 이 파일을 가리킨다.

### 상품 이미지를 CDN URL이 아니라 우리 파일로 둔 이유
MCM CDN URL(`images` 컬럼)은 남의 서버다. 핫링크가 막히거나 상품이 내려가면 데모 당일 이미지가 통째로 사라진다. 직접 준비한 배경 제거 PNG를 쓰면 그 위험이 없고, 화보 합성에 쓸 컷아웃과 상품 목록 이미지가 같은 파일이 된다. 대신 상품당 이미지가 1장이라 상세 화면의 갤러리는 한 장뿐이다.

---

## 2026-08-20 · /events visit_id 선택화 · hotspot 진열대 역추적 · 16유형 제거

### ① /events의 visit_id를 선택으로 — 실시간 챗봇이 죽어 있었다

프론트는 배치를 이렇게 보낸다.

```
POST /events   { "events": [ { ..., "visit_id": "v_abc" } ] }
```

`visit_id`를 **각 이벤트 안**에 넣고 최상위엔 두지 않는다. 그런데 `EventBatchSerializer`는 최상위 `visit_id`를 필수로 봐서 **매 배치가 400**이었다. 프론트는 400이면 큐에 되돌리지도 않고 버린다(`n!==400 && n!==401 && 재적재`).

**그런데 서버에는 이벤트가 쌓이고 있었다.** 운영 서버의 타입별 마지막 수신 시각을 보고서야 이유를 알았다.

```
scene_dwell    17:38:10.226191  ┐
hotspot_click  17:38:10.226206  │ 전부 같은 순간 = 한 덩어리
product_view   17:38:10.226221  │
product_dwell  17:38:10.226236  ┘
visit_end      17:38:10.227669  ← 1.4ms 뒤
store_enter    17:37:12.889320  ← 58초 전
```

**`/finish`에 동봉된 버퍼가 저장되고 있었다.** `FinishRequestSerializer`는 `events`만 받고 `visit_id`를 요구하지 않는다 — URL과 토큰에서 방문을 얻기 때문이다. 같은 body 모양이 한쪽에서는 통과하고 다른 쪽에서는 400이었다.

그래서 **데이터 유실은 없었지만 실시간 경로가 통째로 죽어 있었다.** 관람 중 서버가 보는 이벤트는 `store_enter`·`visit_start` 둘뿐이라:
- 트리거 `evaluate()`가 볼 게 없어 가설 말풍선이 한 번도 안 떴다
- 취향 좌표가 계속 비어 추천 후보가 0건이었다
- `/chat` 프롬프트에 후보가 안 들어가 "어떤 상품 보고 계세요?"로 되물었다

`visit_id`를 선택으로 바꾸고 `assert_own_visit`이 `None`을 받도록 했다. **방문을 확정하는 것은 토큰이고 `visit_id`는 대조용**이므로, 없어도 처리할 수 있는 게 원래 맞다. 명세서 "생각해볼꺼"가 지적한 식별자 3중 노출이 실제 장애로 드러난 셈이다.

하위 호환이다 — 보내던 호출은 그대로 대조받고, 안 보내면 토큰으로 처리한다.

**검증**: 프론트 모양 202 · 명세 모양 202 · 남의 visit_id 403 · 토큰 없음 401. 관람 중 배치를 보내니 가설 말풍선이 실제로 쌓였다.

### ② hotspot_click은 상품이 대상이다

프론트는 상품을 누를 때 `hotspot_click`을 `product_id`만 담아 보낸다. 우리가 `scene_id`를 필수로 요구해서, 프론트가 이런 우회를 넣어 두고 있었다.

```js
if (!scene_id && type === 'hotspot_click') scene_id = scenes[0]?.scene_id || 'sc_01'
```

**4번 진열대를 눌러도 `sc_01`로 기록된다.** 존재하는 ID라 에러도 안 나고 조용히 틀린다.

`SCENE_REQUIRED_TYPES`에서 빼고 `PRODUCT_REQUIRED_TYPES`로 옮겼다. 진열대는 서버가 상품에서 역추적해 **클라이언트가 보낸 값을 덮어쓴다** — 프론트가 틀린 값을 만들 여지 자체를 없애는 쪽이 프론트 수정을 기다리는 것보다 낫다.

**검증**: 4번 진열대 상품에 `sc_01`을 붙여 보내도 `sc_04`로 저장된다. `scene_id` 없이 보내도 202.

### ③ 16유형 제거

리포트가 개편되면서 아무도 안 쓰게 됐는데 코드는 남아 매 리포트마다 돌고 있었다. `Character` 테이블이 0행이라 조회가 실패하고 폴백이 응답에 실렸다.

```json
{"type_code": "CNPD", "name": "분석 중인 타입", "one_liner": "취향을 조금 더 모으는 중이에요."}
```

**리포트는 `ready`인데 화면엔 "분석 중"으로 보였다.**

지운 것: `character.py`, `seed_characters.py`, `Character` 모델, `TasteProfile.axis_scores`·`character_type`, `CharacterAdmin`, `CharacterTest`, `payload["character"]`.

**`TASTE_AXES`는 남겼다.** 16유형 코드를 접는 데만 쓰인 게 아니라 챗봇의 축 확인 트리거와 2택 버튼 문구가 전부 여기서 파생된다. 없앤 것은 "4축을 4글자로 접는 부분"뿐이다.

무드 4종은 추가하지 않았다 — 리포트 파트의 결정이라 담당자와 정할 몫이다.

⚠️ **응답 스키마 변경**: `GET /reports/{slug}`에서 `character` 키가 사라진다. 리포트 화면에 유형 자리가 있으면 빈칸이 된다. 지금은 `top_keywords`와 `summary`가 그 역할을 한다.

### 이번에 두 번 틀렸다
`event_id` 형식이 UUID가 아니라 400이 난다고 했는데, 프론트가 전송 직전에 `Wl()`로 검사해 UUID로 교체하고 있었다. **생성 지점만 보고 조립 함수를 안 봤다.**

그다음엔 "이벤트가 전혀 안 쌓인다"고 했는데 운영 데이터가 반대였다. `/finish` 동봉 경로를 못 본 탓이다.

**미니파이된 번들은 한 지점만 보고 판단하면 안 된다.** 값이 어디서 만들어지는지가 아니라 **어디서 소비되는지**까지 따라가야 하고, 같은 데이터가 들어오는 경로가 둘 이상일 수 있다.

### 검증
테스트 78개 통과. `check`·`makemigrations --check` 통과. 마이그레이션 1건 신규(`analysis.0004`).

---

## 2026-08-20 · 찜 제거 · shift 되살리기 · 프롬프트를 실측으로 판정 (2차)

### ① 죽은 찜이 트리거 하나와 confidence 상한을 잡고 있었다

찜 기능은 기획에서 빠졌고 프론트는 `product_save`를 한 번도 보내지 않는다. 그런데 서버 다섯 곳이 여전히 그 이벤트를 기다리고 있었다. 코드가 틀린 게 아니라 **영원히 참이 되지 않는 조건**이 되어 있었다.

가장 큰 피해는 `shift` 트리거다.

```python
if len(opposite) >= SHIFT_VIEWS and any(p.id in saved for p in opposite):
```

`saved`가 항상 빈 집합이라 **한 번도 발동한 적이 없다.** `shift`는 "아까는 차분한 색이라 하셨는데 지금은 눈에 띄는 색을 보고 계세요"를 묻는, 20~30분 관람 중 마음이 바뀌는 걸 잡는 유일한 장치다. 같은 축을 두 번 묻는 유일한 예외로 설계해 놓고 정작 죽어 있었다.

찜이 하던 역할은 "단순 조회보다 강한 의도 신호"다. 그 자리를 `stored["confirmed"]` — 손님이 "맞아요"로 직접 답한 상품 — 로 바꿨다. **찜보다 강한 신호이고**, 이미 `TasteProfile.vector`에 들어 있어 쿼리도 한 개 줄었다.

두 번째 피해는 조용했다. `CONFIDENCE_SAVE_WEIGHT = 0.10`이 항상 0점이라 **리포트 신뢰도가 구조적으로 90%를 넘을 수 없었다.** 가중치 합이 1.0이어야 만점이 나오는 구조인데 한 항목이 죽으면 그만큼 천장이 내려간다. 0.10을 질문으로 옮겼다 — 사라진 것이 "의도가 분명한 행동"이었으니 성격이 가장 가까운 항목으로 보내는 게 맞다.

지운 것: `EventType.PRODUCT_SAVE`, `PRODUCT_REQUIRED_TYPES` 항목, `taste._behaviour_ids`의 찜 분기, `ProductSignal.saves`·`VisitSignals.total_saves`, `collect`의 집계, `WEIGHT_SAVE`·`CONFIDENCE_SAVE_*`, 리포트 관심 사유의 "찜". 마이그레이션 `events.0003` 신규.

이미 쌓인 `product_save` 행은 지우지 않았다. **append-only는 읽지 않게 되는 것과 없애는 것을 구분한다.** 읽는 코드가 없으면 그걸로 충분하다.

**검증**: `confirmed` 없으면 `None`(게이트 유지), 있으면 발동 — "아까는 절제된 느낌 쪽이라고 하셨는데, 지금은 개성 있는 결 쪽을 보고 계세요." 찜 없이 confidence 1.0 도달. `product_save` 전송 시 400. 리포트 `ready`.

### ② 프롬프트를 눈으로 판단해서 틀렸다

`_describe()`가 만드는 상품 블록을 읽고 결함 여섯 개를 찾았다. 실제로 나가는 문자열은 이랬다.

```
- 설명:
- 가격: 1,690,000원
- material: 스페인산 엠보스드 레더
- 특징: 핑크 / 그레인 레더 / ...
- capacity: -
- 가격: 1,690,000원입니다. 오래 두고 쓰는 가격대예요.
- 디자인 의도: 뮌헨 아이콘의 재 탄생
- 브랜드 스토리: 뮌헨 아이콘의 재 탄생
```

14줄 중 6줄이 빈칸·중복·모순이었다. 특히 축(`그레인 레더`)과 표기값(`스페인산 엠보스드 레더`)이 60개 중 57개에서 다른데 같은 층위로 나열돼 있었다. **"모델이 소재를 두 개로 보고 손님에게 틀린 소재를 말할 것"** 이라고 판단하고, 실물을 앞에 두고 축을 `(분류상 X)`로 붙이는 안을 만들었다.

블록을 8줄로 줄이고(글자 41% 감소) 라벨 충돌을 없앤 뒤, 같은 질문을 상품 3종 × 4회 = 12회 던져 A/B로 확인했다.

| 변형 | 실물 소재 정답 | 내부 축 누출 |
|---|---|---|
| 구 블록 | 12/12 | 0/12 |
| 신 블록 + `(분류상 X)` | 12/12 | **10/12** |
| 신 블록, 축 제거 | 12/12 | 0/12 |

**진단이 틀렸다.** 구 블록도 12/12 정답이었다. 모델은 이미 `preset_answers`를 축보다 우선하고 있었고, 내가 위험이라고 부른 모순은 오답을 만들지 않았다.

**그리고 내 수정이 새 문제를 만들었다.** `(분류상 코팅 캔버스)`를 83% 확률로 손님에게 그대로 옮겼다 — 추천 엔진 내부 분류를 매장 손님이 듣게 되는 것이다.

같은 방식으로 나머지도 재봤다. 진열대 이름을 후보 블록에 넣는 안은 성별 교차 오추천을 5회씩 측정해 **0/5 → 0/5**, 효과가 없었다. `_describe_store`가 이미 진열대 이름을 주고 있어 모델이 원래 틀리지 않았다. 첫 측정("6번 진열대를 안내하는가")은 두 조건 다 4/4라 **아무것도 구분하지 못하는 지표**였다.

결국 블록 재구성·헬퍼·진열대 이름을 전부 되돌렸다. 41% 축소는 실재하지만 정확도 근거가 없는 변경이다.

남긴 것은 하나다. 가드레일의 `"여행 갈 때"는 여행용 상품으로` → `수납이 큰 백팩·토트로`. `use_case: travel` 재고가 0이라 없는 물건을 가리키던 문장이고, 사전은 이미 백팩·토트로 보내고 있어 프롬프트만 뒤처져 있었다. 실측 1/4 → 0/4.

**프롬프트는 읽어서 판단하면 안 된다. 돌려서 판단해야 한다.** 지저분한 프롬프트와 틀린 답은 다른 문제다. 지저분함을 근거로 정확도를 추정한 것이 이번 실수의 본질이고, 그 추정 위에 세운 해법이 실제로는 해로웠다.

지표도 함께 설계해야 한다. "6번을 안내하는가"처럼 양쪽 다 통과하는 지표는 검증이 아니라 통과 의식이다. **틀렸을 때 틀렸다고 나오는 지표인지**를 먼저 확인해야 한다.

### 데이터 결함 (코드가 아니라 데이터 작업)

`demo.json` 60개를 전수 감사했다. 구조는 깨끗하다 — id 중복 0, 진열대별 `no` 연속, 이미지·외부 URL·가격 정상, 8개 축 480/480 전부 채워져 있다.

텍스트 필드에 넷이 남아 있다.

| 결함 | 빈도 |
|---|---|
| `llm_context` 빈 문자열 | 60/60 |
| `story` == `preset_answers.design_intent` (글자까지 동일) | 60/60 |
| `attributes.capacity`가 `"-"` | 48/60 |
| 상품명에 HTML 엔티티 `&reg;` | p_209, p_602 |

앞 셋은 위 실측에서 오답을 만들지 않았다. **넷째만 성격이 다르다 — 챗봇이 소리 내어 읽는 값이다.**

재고 0인 축 값도 다섯이다: `travel`, `bold_statement`, `visetos_mix`, `suede`, `shoulder`/`wallet`. 사전이나 분석이 이 값을 가리키면 결과가 빈다. `pattern`은 `visetos_monogram`이 77%라 변별력이 거의 없다.

`cutout_url`이 가리키는 파일이 60/60 없다. `media/`는 `.gitignore` 대상이라 서버에 직접 올려야 한다. 경로 계산 버그를 의심했지만 Django가 `MEDIA_URL`을 `/media/`로 정규화해서 정상이었다. 화보 합성은 파일이 없으면 조용히 건너뛰므로 **배포 시 확인이 필요하다.**

### 검증

`ruff` 통과. `check`·`makemigrations --check` 통과. 테스트 78개 통과. 마이그레이션 1건 신규(`events.0003`).


---

## 2026-08-20 · 상품 데이터 복구 · 루트 스크립트 정리 (3차)

### ① demo.json은 생성기가 만든 파일이 아니었다

`import_products_csv.py`가 CSV에서 `demo.json`을 만든다. 그런데 저장소에 있던 파일은 그 생성기의 출력이 아니었다. 증거가 넷이다.

| | 생성기 출력 | 저장소 파일 |
|---|---|---|
| `_comment` | `"직접 고치지 말고 CSV를 고친 뒤 다시 돌릴 것"` | 없음 |
| `attributes` 키 | 9개(`dimensions`·`lining`·`origin`·`sku`…) | 4개(`color`·`material`·`size`·`capacity`) |
| `preset.price` | `"…원입니다. 지금 구매하실 수 있어요."` | `"…원입니다. 오래 두고 쓰는 가격대예요."` |
| `cutout_url` | `/media/products/` | `/media/cutouts/` |

**생성기가 있는데 안 쓰이고 있었다.** 그래서 결함이 생겼다 — `llm_context`가 60개 전부 비어 있었고(생성기는 CSV의 `description`+`features`로 채운다), 상품명에 `&reg;`가 남았고(생성기의 `clean()`이 푼다), `capacity`에 `"-"` 48건이 들어 있었다(생성기는 그 키를 만들지 않는다).

### ② 그런데 재생성이 답은 아니었다

원본 CSV(`mcm_60.csv`)로 다시 돌려 현재 파일과 비교하니 **축이 71/480 바뀌었다.** `pattern` 25건, `mood` 21건, `silhouette` 15건.

`mood`와 `pattern`은 진영(`CAMPS`)·추천 가중치·트리거가 모두 걸리는 축이라, 이 둘이 흔들리면 어제 검증한 추천과 트리거가 통째로 다시 검증 대상이 된다. 게다가 생성기 쪽이 옳다는 보장도 없었다 — 규칙에 순서 버그가 있었다(아래 ④).

재생성본이 **더 나쁜 부분**도 있었다.

```
preset.price  재생성 "…원입니다. 지금 구매하실 수 있어요."
```

시스템 프롬프트는 "재고·할인·입고는 단정하지 말고 직원에게 확인하도록 안내"하라고 지시한다. 그 옆에 재고 상태를 넣으면 정면으로 충돌한다. `attributes`에 붙는 `sku`·`availability`·`collection`도 그대로 프롬프트로 샌다.

그래서 **손으로 못 만드는 텍스트만 가져오고 축 8개는 건드리지 않는 병합**으로 갔다.

가져온 것: `llm_context`(60건) · `name`(2건) · `preset.design_intent`(60건).
지킨 것: 축 8개 · `attributes`(`"-"`만 삭제) · `preset.price`/`material` · `story` · `cutout_url` · `images` · `price`.

**검증**: `category`·`material`·`pattern`·`silhouette`·`mood`·`price_band`·`use_case` 변경 **0건**.

### ③ design_intent는 프롬프트가 아니라 손님이 보는 문자열이다

`story`와 `design_intent`가 60/60 글자까지 같았다. 처음엔 프롬프트 중복 문제로 봤는데, 아니었다.

```python
answer = ChatLog.objects.create(role=Role.PRESET, content=product.preset_answers[preset_key])
```

**「디자인 의도」 버튼을 누르면 이 문자열이 LLM을 거치지 않고 그대로 말풍선이 된다.** 손님이 보는 답이 "뮌헨 아이콘의 재 탄생" 일곱 글자였다. 「가격」·「재질」 버튼은 실질적인 답을 주는데 이것만 카피였다.

CSV에 갈래가 있었다 — `summary`는 짧은 카피, `description`은 실제 설명 문단. `description`을 통째로 붙이지 않은 이유는 중앙값 139자·최대 257자라 서서 읽기에 길고, 글자 수로 자르면 문장이 중간에 끊기기 때문이다. **마침표 기준 첫 문장까지만** 가져왔다.

붙여 보고 한 번 더 다듬었다. **두 필드의 역할이 행마다 뒤집혀 있었다.**

```
p_101  summary=카피("뮌헨 아이콘의 재 탄생")      description=스펙 프로즈
p_201  summary=스펙("피라미드 스터드…백팩")       description=카피("글로벌 노마드의 자유로운 정신…")
```

순서를 바꾸면 다른 행이 깨지므로 이음새만 맞췄다 — 앞 문장이 이미 마침표로 끝나면 `—` 대신 공백으로 잇는다.

### ④ 생성기 규칙 두 개를 고쳤다 (지금 데이터는 안 바뀐다)

```python
MATERIAL_RULES = (("비세토스", "coated_canvas"), ..., ("나파", "smooth_leather"), ...)
```

첫 매치가 이기는데 판정 대상이 `소재 + 상품명`이다. 소재가 **나파 레더**인 참 6건이 이름에 "비세토스"가 있다는 이유로 **코팅 캔버스**가 됐다. 구체적인 소재 이름을 앞으로 옮겼다.

이번 병합은 축을 안 가져오므로 **지금 fixture는 한 건도 안 바뀐다.** 다음에 CSV로 재생성할 때를 위한 수정이다.

### ⑤ 축에 없는 색이 조용히 black이 되어 있었다

`COLOR_MAP`이 `.get(value, "black")`으로 폴백한다. 축 `color`와 `attributes.color`를 대조하니 17건이 달랐는데, 15건은 합리적인 접기였고(`brown→cognac`·`blue→navy`·`gold→metallic`) 둘이 틀렸다.

```
p_103  orange 그레인 가죽 토트  → black    → cognac 으로 수정
p_307  green(카키·모스) 쇼퍼    → black    → beige  으로 수정
```

코냑은 가죽업계에서 황갈색을 부르는 말이라 오렌지 가죽에 가장 가깝다. 생성기의 `orange→red`보다 정확하다. `attributes.color`에 원래 색이 남아 있어 **화면과 챗봇은 맞게 말하고 있었다** — 틀린 건 추천 점수와 진영 판정뿐이었다.

수정 직후 코냑 lock으로 추천을 돌리니 `Milla 그레인 가죽 토트 / 코냑`이 1순위로 나왔다.

### ⑥ 루트 스크립트가 테스트를 오염시키고 있었다

`manage.py test`를 앱 이름 없이 돌리면 항상 1건 실패했다. 원인이 둘이었다.

- `test_crawler.py` — `mcm_crawler2.py`와 **마지막 개행 하나만 다른 복사본**이다. 자기 docstring의 사용법조차 `python mcm_crawler2.py`를 가리킨다. `bs4` 미설치로 임포트 실패.
- `test_access.py` — `__main__` 가드가 없어서 **임포트만으로 스크립트 전체가 실행된다.** 테스트를 돌릴 때마다 MCM 접속 진단 배너가 찍히고 있었다.

두 번째가 더 위험하다. 지금 네트워크를 안 때리는 건 순전히 `requests`·`curl_cffi`·`playwright`가 venv에 없어서다. **누군가 `pip install requests`를 하는 순간 `manage.py test`가 매번 mcmworldwide.com에 실제 요청을 보낸다.**

`test_crawler.py`는 지웠고, `test_access.py`는 `check_mcm_access.py`로 이름만 바꿨다. 죽은 코드가 아니라 `mcm_crawler2.py`가 에러 메시지에서 두 번 안내하는 짝 도구다. `test_` 접두사만 떼면 discovery에 안 걸린다.

`AGENTS.md`는 `CLAUDE.md`와 제목 한 줄만 다른 중복이지만 **손대지 않았다.** 다른 AI 도구가 읽는 표준 관례라 의도적 중복일 수 있고, 그건 팀이 정할 몫이다.

### 남은 것

`mcm_60.csv`가 저장소에 없다(로컬 `~/Downloads`에만 있다). 새로 클론하면 `import_products_csv`를 돌릴 수 없다. 공개된 상품 정보 60행이라 시크릿 문제는 없으므로 **커밋할지 정해야 한다.**

### 검증

`ruff`·`check`·`makemigrations --check` 통과. **`manage.py test`가 앱 지정 없이 78건 통과**(전에는 항상 1건 실패).

프롬프트 실측: 가격·크기·위치가 그대로 정확하고, 전에는 답하지 못하던 질문이 답된다 — "어떤 특징이 있어요?"(탈부착 가죽 숄더 스트랩·내부 구획 구조) · "안감은 뭐예요?"(스웨이드 마감 극세사) · "어디서 만들어요?"(이탈리아).

상품 블록은 평균 417자 → 897자로 커졌다. `llm_context`가 실제 상품 설명이라 늘어난 것이고, 그만큼 답할 수 있는 범위가 넓어졌다.

---

## 2026-08-20 · 누락 점검 — 16유형 잔재와 낡은 문서 (4차)

지금까지의 작업에서 빠진 것이 없는지 훑었다. 두 종류가 나왔다.

### ① 16유형을 지우면서 함수 둘을 남겼다

모델·시리얼라이저·커맨드·마이그레이션까지 정리했는데 정작 코드를 만드는 함수가 남아 있었다.

```python
def character_code(taste: Taste) -> str:
    """16유형 코드. 무효 축은 소문자로 표시해 "판정 보류"를 드러낸다."""
```

참조 0건. `Hypothesis.is_axis_confirm`도 같이 참조 0건이었다. 둘 다 지웠다.

`CAMPS`·`CORE_AXES`는 남겼다. 16유형 코드를 접는 데만 쓰인 게 아니라 진영 판정
(`valid_axes`)과 `avoidance`·`shift`의 "반대 진영" 계산이 여기서 나온다.

**찾은 방법**: AST로 함수 정의를 모으고 저장소 전체에서 이름 등장 횟수를 세어 1회
(= 정의 자신)뿐인 것을 골랐다. DRF가 이름 규칙으로 부르는 훅(`get_permissions`·
`validate_scene_id`·`get_cutout_url`)이 함께 걸리므로 눈으로 걸러야 한다.

### ② 문서가 바뀐 계약을 옛날대로 적어두고 있었다

죽은 코드보다 이쪽이 위험하다. **프론트가 문서를 보고 구현하면 400을 맞는다.**

| 문서 | 적혀 있던 것 | 실제 |
|---|---|---|
| 필드_가이드 | `product_save`에 `product_id` 필수 | 이벤트 타입이 없다. 보내면 400 |
| 필드_가이드 | `hotspot_click`에 **`scene_id` 필수** | `product_id` 필수로 바뀌었다 |
| 필드_가이드 | 리포트에 `character_type` | 응답에서 제거됐다 |
| 챗봇_로직 | shift 조건 = 조회 3개 + **찜 1개** | `맞아요`로 확인한 상품 1개 |
| 챗봇_로직 | 관심 집합 진입에 **찜** | 제거됐다 |
| 명세_액션메시지 | 트리거 6종 | **`shift`가 빠져 있었다** (7종) |

`hotspot_click` 건은 예상 밖이었다. 서버는 오전에 고쳤는데 문서는 그대로여서,
**고친 쪽과 안내하는 쪽이 반대**로 남아 있었다.

`명세_액션메시지`의 `color_neutral`·`color_vivid`는 지우지 않고 **도달 불가 경고**를
달았다. 서버가 받아 처리는 하는데 내놓는 트리거가 없다 — 되돌려 확인 트리거가
구현되지 않은 채 선택지만 남은 상태다. 트리거를 만들지 선택지를 지울지는 정해야 한다.

### 문서를 코드와 대조해서 고쳤다

문서에 숫자를 옮겨 적을 때는 코드에서 다시 읽어 맞췄다 — `SHIFT_VIEWS`,
`SUGGESTION_LIMIT`, `PRODUCT_REQUIRED_TYPES`, 트리거가 실제로 내놓는 `option` 값.
**문서를 고치면서 또 틀리는 것이 가장 흔한 실패**라서다.

### 안 한 것

- `.env.example`의 `DOMAIN`·`POSTGRES_*` — "docker compose가 읽는다"고 적혀 있지만
  이 프로젝트에 compose 파일이 없다(GitHub Actions + systemd + nginx). 배포 파트라 남겨 둔다.
- `mcm_60.csv` 커밋 — 새로 클론하면 `import_products_csv`를 돌릴 수 없다. 판단 보류.
- 체류시간 판정의 불일치 — 리포트는 상품별로 **합산**하는데 관심 집합·트리거는
  **개별 이벤트**만 본다. 채팅을 오가면 이벤트가 쪼개져, 같은 24초를 봐도 한 번에 본
  손님은 집합에 들어가고 12초씩 두 번 본 손님은 못 들어간다. 지금은 현행 유지.

### 검증

`ruff`·`check`·`makemigrations --check` 통과. 테스트 78개 통과. OpenAPI 스키마 경고 0건.

---

## 2026-08-20 · 화보 이미지 역할 프롬프트 정비

화보 생성에 넣는 세 이미지를 역할별로 분리했다. Image 1은 인물의 정체성·체형 보존,
Image 2는 편집 화보의 구도·패널·콜라주 구조 참고, Image 3은 제품 형태·재질·착용 상호작용
보존을 담당한다.

기존에는 레퍼런스의 글자를 막기 위해 찢어진 종이 프레임과 콜라주 테두리까지 금지했다.
그 결과 레퍼런스의 핵심 연출도 함께 버려졌다. 읽을 수 있는 텍스트·로고·워터마크만 금지하고,
빈 타이포 영역·패널·테두리·콜라주 분할은 글자 없는 추상 레이아웃으로 재현할 수 있게 바꿨다.

레퍼런스가 있을 때 기존의 고정 좌측 여백·성당 배경 지시는 제거했다. 특정 시안의 배경을
코드가 강제하면 업로드한 다른 레퍼런스와 충돌하기 때문이다. 레퍼런스가 없을 때만 최소한의
스튜디오 화보 방향을 사용하는 폴백을 둔다.

### 검증

`venv\\Scripts\\ruff.exe check apps/lookbook/prompts.py apps/lookbook/tests.py` 통과.
로컬 가상환경의 기반 Python 실행 파일이 없어 Django 테스트·check·migration 검사는 실행하지
못했다. 배포 또는 정상 Python 환경에서 `python manage.py test apps.lookbook.tests.PromptTest`,
`python manage.py check`를 다시 실행해야 한다.

---

## 2026-08-20 · 코드리뷰 대비 정비 + 프론트-백엔드 계약 전수조사 반영

리뷰어는 GitHub main을 클론해 기본 설정으로 실행한다는 관점에서 정비했다.

- **HTTP 계약 테스트 17개**(`api/tests.py`) — 시리얼라이저 유닛 테스트가 못 잡는
  회귀(퍼미션이 AllowAny로 풀리는 실수, 에러 포맷 변경, IDOR, 멱등성)를 실제 라우팅
  전체를 통과시켜 잡는다. 앱별 tests.py가 아니라 api/에 둔 이유: 인증·스코핑이
  api/ 레이어의 관심사이고, 테스트가 앱 경계를 가로지르기 때문.
- **업로드 스로틀** — `PUT /uploads/{key}`는 무인증이라 KEY_PATTERN에 맞는 키를
  지어내 5MB씩 채울 수 있다. 유일한 방어가 속도 제한이다. receive를 IP당 60/min으로
  느슨하게 둔 것은 매장 공용 와이파이에서 여러 손님이 한 IP를 쓰기 때문.
- **purge 타이머·deploy 스크립트 레포 편입** — 명령만 있고 호출자가 없어 얼굴 사진이
  서버에 쌓이고 있었다. 서버에만 있는 배포 스크립트는 레포만 보고 재현할 수 없었다.
- **이벤트 배치: 전체 400 → 건별 rejected** — 프론트가 400에 큐를 통째로 비우므로,
  잘못된 id 하나가 정상 이벤트 전부(그리고 finish까지)를 날렸다. "조용히 버리면
  수치가 틀어진다"는 원래 우려는 rejected 카운트와 warning 로그로 대체.
- **왕복(A→B→A) 트리거 복구** — 프론트 재마운트 중복 전송이 '마지막 3건 일치' 판정을
  영구 실패시켰다. 연속 중복 압축 + 최근 10건 창 탐색으로 변경. 버린 대안: 성립 시점에
  vector에 박제 — 상태가 늘고, 창 탐색만으로 충분했다.
- **FAKE 화보 placeholder 의존 제거** — 존재하지 않는 정적 파일 대신 단색 인물판을
  실제 합성·저장 경로에 태운다. 클론 직후에도 화보 E2E가 눈으로 확인된다.
- **on_commit은 넣지 않았다** — finish()가 @transaction.atomic이고 enqueue는 리턴
  (=커밋) 후 호출되며 ATOMIC_REQUESTS도 꺼져 있어 race가 없다. 점수용 장식이라 보류.
- 프론트 수정 4건은 패치로 전달(dwell 3중 전송, 클릭 로그 필터, 계약 정합, 분 표시).

### 검증

ruff 통과 · `makemigrations --check` 통과 · 테스트 185개 전부 통과(계약 테스트 포함).
