# 인증서 발급·갱신을 Caddy가 자동으로 한다. 80·443이 열려 있어야 발급이 된다.
{$DOMAIN} {
	encode zstd gzip

	# SSE(챗봇 스트리밍)가 버퍼링되지 않게 한다. 이걸 빠뜨리면 응답이 다 끝난 뒤
	# 한 번에 도착해서 "스트리밍이 안 되는" 증상이 나온다.
	reverse_proxy web:8000 {
		flush_interval -1
	}
}
